import React, { useState, useEffect, useMemo, useRef } from "react";
import * as XLSX from "xlsx";
import {
  Package, ClipboardList, BarChart3, Plus, Search, AlertTriangle,
  Trash2, ChevronDown, ChevronRight, X, TrendingUp, BedDouble,
  PackagePlus, Truck, Check, Pencil, ArrowRightLeft, Warehouse,
  FileSpreadsheet
} from "lucide-react";

const MERINO_LOGO =
  "data:image/svg+xml;base64,";

const APP_ICON = "data:image/png;base64,";

// ---------- seed data ----------

const CATEGORIES = ["Продукты", "Санузел и комплектация гостя", "Кухня", "Стирка", "Хозяйственные расходники", "Средства для уборки"];

const CATEGORY_EMOJI = {
  "Продукты": "🛒",
  "Санузел и комплектация гостя": "🚿",
  "Кухня": "🍽️",
  "Стирка": "🧺",
  "Хозяйственные расходники": "🧹",
  "Средства для уборки": "🧴",
};

const seedItems = [
  // Продукты — соль, сахар и перец выдаются целой закупленной упаковкой,
  // без развеса, поэтому единица учёта для них — "пачка", а не "100 г".
  { id: "i1", code: "ПР-001", name: "Соль", category: "Продукты", unit: "пачка", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i2", code: "ПР-002", name: "Сахар", category: "Продукты", unit: "пачка", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i3", code: "ПР-003", name: "Перец", category: "Продукты", unit: "пачка", quantity: 0, minQuantity: 5, unitCost: 0 },
  { id: "i4", code: "ПР-004", name: "Чай", category: "Продукты", unit: "пакетик", quantity: 0, minQuantity: 100, unitCost: 0 },
  { id: "i5", code: "ПР-005", name: "Кофе растворимый", category: "Продукты", unit: "100 г", quantity: 0, minQuantity: 8, unitCost: 0 },
  { id: "i6", code: "ПР-006", name: "Кофе молотый для кофемашин", category: "Продукты", unit: "100 г", quantity: 0, minQuantity: 8, unitCost: 0 },
  { id: "i7", code: "ПР-007", name: "Кофейные капсулы маленькие", category: "Продукты", unit: "шт", quantity: 0, minQuantity: 80, unitCost: 0 },
  { id: "i8", code: "ПР-008", name: "Кофейные капсулы большие", category: "Продукты", unit: "шт", quantity: 0, minQuantity: 50, unitCost: 0 },
  { id: "i9", code: "ПР-009", name: "Печенье", category: "Продукты", unit: "шт", quantity: 0, minQuantity: 100, unitCost: 0 },

  // Санузел и комплектация гостя
  { id: "i10", code: "СК-001", name: "Туалетная бумага", category: "Санузел и комплектация гостя", unit: "рулон", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i11", code: "СК-002", name: "Гель для душа", category: "Санузел и комплектация гостя", unit: "100 мл", quantity: 0, minQuantity: 25, unitCost: 0 },
  { id: "i12", code: "СК-003", name: "Шампунь", category: "Санузел и комплектация гостя", unit: "100 мл", quantity: 0, minQuantity: 25, unitCost: 0 },
  { id: "i13", code: "СК-004", name: "Кондиционер для волос", category: "Санузел и комплектация гостя", unit: "100 мл", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i14", code: "СК-005", name: "Жидкое мыло", category: "Санузел и комплектация гостя", unit: "100 мл", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i15", code: "СК-006", name: "Зубной набор", category: "Санузел и комплектация гостя", unit: "набор", quantity: 0, minQuantity: 40, unitCost: 0 },
  { id: "i16", code: "СК-007", name: "Одноразовые тапочки", category: "Санузел и комплектация гостя", unit: "пара", quantity: 0, minQuantity: 50, unitCost: 0 },
  { id: "i17", code: "СК-008", name: "Ватные диски", category: "Санузел и комплектация гостя", unit: "шт", quantity: 0, minQuantity: 60, unitCost: 0 },
  { id: "i18", code: "СК-009", name: "Ватные палочки", category: "Санузел и комплектация гостя", unit: "шт", quantity: 0, minQuantity: 60, unitCost: 0 },

  // Кухня
  { id: "i19", code: "КХ-001", name: "Средство для мытья посуды", category: "Кухня", unit: "100 мл", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i20", code: "КХ-002", name: "Таблетки для посудомоечной машины", category: "Кухня", unit: "таблетка", quantity: 0, minQuantity: 40, unitCost: 0 },
  { id: "i21", code: "КХ-003", name: "Губка для посуды", category: "Кухня", unit: "шт", quantity: 0, minQuantity: 30, unitCost: 0 },
  { id: "i22", code: "КХ-004", name: "Бумажные полотенца", category: "Кухня", unit: "рулон", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i23", code: "КХ-005", name: "Салфетки столовые", category: "Кухня", unit: "шт", quantity: 0, minQuantity: 100, unitCost: 0 },

  // Стирка
  { id: "i24", code: "СТ-001", name: "Стиральный порошок", category: "Стирка", unit: "100 г", quantity: 0, minQuantity: 10, unitCost: 0 },
  { id: "i25", code: "СТ-002", name: "Капсулы для стирки", category: "Стирка", unit: "шт", quantity: 0, minQuantity: 25, unitCost: 0 },

  // Хозяйственные расходники
  { id: "i26", code: "ХР-001", name: "Мусорные пакеты", category: "Хозяйственные расходники", unit: "пакет", quantity: 0, minQuantity: 50, unitCost: 0 },
  { id: "i27", code: "ХР-002", name: "Тряпочки / хозяйственные салфетки", category: "Хозяйственные расходники", unit: "шт", quantity: 0, minQuantity: 25, unitCost: 0 },
  { id: "i28", code: "ХР-003", name: "Освежитель воздуха", category: "Хозяйственные расходники", unit: "шт", quantity: 0, minQuantity: 12, unitCost: 0 },

  // Средства для уборки
  { id: "i29", code: "СУ-001", name: "Санэкс / средство для унитаза", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 10, unitCost: 0 },
  { id: "i30", code: "СУ-002", name: "Средство для мытья полов", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i31", code: "СУ-003", name: "Универсальное средство для поверхностей", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i32", code: "СУ-004", name: "Средство для ванной / сантехники", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i33", code: "СУ-005", name: "Средство для стекол и зеркал", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 15, unitCost: 0 },
  { id: "i34", code: "СУ-006", name: "Антижир / средство для кухни", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 12, unitCost: 0 },
  { id: "i35", code: "СУ-007", name: "Средство от известкового налёта", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 12, unitCost: 0 },
  { id: "i36", code: "СУ-008", name: "Дезинфицирующее средство", category: "Средства для уборки", unit: "100 мл", quantity: 0, minQuantity: 10, unitCost: 0 },
  { id: "i37", code: "СУ-009", name: "Меламиновые губки", category: "Средства для уборки", unit: "шт", quantity: 0, minQuantity: 20, unitCost: 0 },
  { id: "i38", code: "СУ-010", name: "Губки хозяйственные", category: "Средства для уборки", unit: "шт", quantity: 0, minQuantity: 30, unitCost: 0 },
  { id: "i39", code: "СУ-011", name: "Перчатки одноразовые", category: "Средства для уборки", unit: "пара", quantity: 0, minQuantity: 12, unitCost: 0 },
  { id: "i40", code: "СУ-012", name: "Тряпки / салфетки для уборки", category: "Средства для уборки", unit: "шт", quantity: 0, minQuantity: 25, unitCost: 0 },
];

// Остатки обнулены — история поступлений и выдач начинается с чистого листа.
// Первую реальную партию по каждой позиции нужно провести через «Оприходовать
// партию»: там же и сложится настоящая себестоимость единицы.
const seedReceipts = [];

const seedIssues = [];

// ---------- реестр объектов ----------
// Синхронизировано с "Картой объектов" (Google Sheets): номер + название.
// Объекты со временем появляются и выбывают — поэтому у каждого есть флаг
// active, а не физическое удаление. В выдаче предлагаются только активные
// объекты, но название на момент выдачи всегда сохраняется в самой записи
// выдачи (в отчётах), так что ретроспектива по выбывшим объектам не теряется,
// даже если объект потом уберут из активного списка или удалят совсем.
const seedObjects = [
  ["01", "Зевс"], ["02", "Ван Гог"], ["03", "Черная речка"], ["04", "Астра"],
  ["05", "София"], ["06", "Нежность"], ["07", "Зеленое сияние"], ["08", "Ясный день"],
  ["09", "Матисс"], ["10", "Золото (Аквалина)"], ["11", "Высота"], ["12", "Млечный путь"],
  ["13", "Алия"], ["14", "Аврора"], ["15", "Ярд"], ["16", "Куб"],
  ["17", "Лето"], ["18", "Мир"], ["19", "Роза"], ["20", "Мягкое утро"],
  ["21", "Чувство"], ["22", "Авенир"], ["23", "Танец Волн"], ["24", "Баланс"],
  ["25", "Довлатов"], ["26", "Граффит"], ["27", "Велюр"], ["28", "Нобу"],
  ["29", "Алиса"], ["30", "Проза"], ["31", "Сиена"], ["32", "Июль"],
  ["33", "Мята"], ["34", "Фиолет"], ["35", "Астрид"], ["36", "Альба"],
  ["37", "Галактика"], ["38", "КуМир"], ["39", "Дзен"], ["40", "Дыхание"],
  ["41", "Моно"], ["42", "Лилу"], ["43", "Гагарин"], ["44", "Апельсин"],
  ["45", "Геомеотрия"], ["46", "Черника"], ["47", "Янтарь"], ["48", "Мандарин"],
  ["49", "Анфилада"], ["50", "Море"], ["51", "Камелия"], ["52", "Джапанди"],
  ["53", "Комфорт"], ["54", "Амели"], ["55", "Элиза"], ["56", "Мёд"],
  ["57", "Терракота"], ["58", "Аура"], ["59", "Своё место"], ["60", "Крылья"],
  ["61", "Оливия"], ["62", "Флора"], ["63", "Изумруд"], ["64", "Мягкий свет"],
  ["65", "Искусство"], ["66", "Натали"], ["67", "Солнце"], ["68", "Мейсон"],
  ["69", "Любовь это..."], ["70", "Луна"], ["71", "Будущее"], ["72", "Вид на Лахту"],
  ["73", "Бисквит"], ["74", "Поцелуй"], ["75", "Журавли"], ["76", "Орех"],
  ["77", "Фрида"], ["78", "Мансарда"], ["79", "Терра"], ["80", "Селеста"],
  ["81", "Клинкер"], ["82", "Полли"], ["83", "Лагуна"], ["84", "Локо"],
  ["85", "Авокадо"], ["86", "Капелла"], ["87", "Мелисса"],
].map(([number, name], idx) => ({ id: `o${idx + 1}`, number, name, active: true }));

// ---------- общескладской инвентарь ----------
// Многоразовое оборудование (раскладушки, обогреватели, одеяла, подушки и т.п.),
// которое не расходуется, а перемещается между складом и объектами по запросу
// гостя и должно вернуться обратно. В отличие от расходников, тут важно не
// "сколько потрачено", а "где сейчас находится" — поэтому ведём отдельный
// реестр типов инвентаря и журнал перемещений, а текущее местоположение
// каждой единицы считается из журнала (склад минус все перемещения туда/обратно).
const seedAssets = [
  { id: "a1", name: "Раскладушка", totalQuantity: 10 },
  { id: "a2", name: "Обогреватель", totalQuantity: 8 },
  { id: "a3", name: "Одеяло", totalQuantity: 20 },
  { id: "a4", name: "Подушка", totalQuantity: 20 },
];

const seedAssetTransfers = [];

// Список складов как локаций для инвентаря — как и объекты, список можно
// пополнять (администратор может добавить третий склад и т.д.). Ключ первого
// склада зафиксирован как "warehouse" — с него исторически начинается расчёт
// totalQuantity в getAssetDistribution, менять его нельзя, не сломав данные.
const seedWarehouses = [
  { id: "warehouse", name: "Склад 1" },
  { id: "warehouse2", name: "Склад 2" },
];

// Справочник вариантов закупки по позиции ("где смотрели и почём", ещё до
// реальной покупки) — отдельно от "Истории поступлений", в которой уже
// зафиксированные, реально свершившиеся закупки.
const seedPurchaseOptions = [];

// ---------- helpers ----------

const rub = (n) =>
  new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 0 }).format(Math.round(n)) + " ₽";

const num = (n) => new Intl.NumberFormat("ru-RU").format(n);

// Единицы вида "100 г" / "100 мл" сами начинаются с числа, поэтому просто
// склеивать их с количеством нельзя — "40 100 г" читается как одно число.
// Разделяем знаком "×": "40 × 100 г".
const fmtQty = (quantity, unit) => `${num(quantity)}${/^\d/.test(unit) ? " × " : " "}${unit}`;

// Текущее распределение единиц инвентаря по местам: старт — весь totalQuantity
// на складе, дальше применяем все перемещения (откуда минус, куда плюс).
// Порядок перемещений не важен — это просто сумма подписанных дельт.
const getAssetDistribution = (asset, transfers) => {
  const dist = { warehouse: asset.totalQuantity };
  transfers
    .filter((t) => t.assetId === asset.id)
    .forEach((t) => {
      dist[t.fromKey] = (dist[t.fromKey] || 0) - t.quantity;
      dist[t.toKey] = (dist[t.toKey] || 0) + t.quantity;
    });
  return dist;
};

const todayISO = () => new Date().toISOString().slice(0, 10);

const fmtDate = (iso) => {
  const d = new Date(iso + "T00:00:00");
  return d.toLocaleDateString("ru-RU", { day: "2-digit", month: "2-digit", year: "numeric" });
};

// Полная метка даты-времени (для журнала изменений — в отличие от выдач и
// поступлений, тут за один день может случиться несколько правок подряд).
const nowStamp = () => new Date().toISOString();

const fmtDateTime = (iso) => {
  const d = new Date(iso);
  return d.toLocaleString("ru-RU", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit" });
};

let idCounter = 100;
const nextId = (prefix) => `${prefix}${idCounter++}`;

const MONTH_NAMES = [
  "Январь", "Февраль", "Март", "Апрель", "Май", "Июнь",
  "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь",
];

// ---------- экспорт в Excel ----------

const downloadWorkbook = (sheets, filename) => {
  const wb = XLSX.utils.book_new();
  sheets.forEach(({ name, rows }) => {
    const ws = XLSX.utils.json_to_sheet(rows);
    XLSX.utils.book_append_sheet(wb, ws, name.slice(0, 31));
  });
  XLSX.writeFile(wb, filename);
};

// Полная выгрузка всех накопленных данных склада: каталог, журнал выдач,
// журнал поступлений и реестр объектов — каждое на отдельном листе.
const exportAllData = (items, issues, receipts, objects, purchaseOptions = []) => {
  const itemRows = items.map((i) => ({
    "Наименование": i.name,
    "Категория": i.category,
    "Ед. изм.": i.unit,
    "Остаток": i.quantity,
    "Мин. запас": i.minQuantity,
    "Себестоимость ед., ₽": Math.round(i.unitCost * 100) / 100,
    "Стоимость остатка, ₽": Math.round(i.quantity * i.unitCost),
  }));

  const issueRows = issues.map((s) => ({
    "Дата": fmtDate(s.date),
    "Объект (номер)": s.roomNumber,
    "Объект (название)": s.roomName,
    "Материал": items.find((i) => i.id === s.itemId)?.name || "—",
    "Количество": s.qty,
    "Себестоимость, ₽": s.cost,
    "Комментарий": s.note || "",
  }));

  const receiptRows = receipts.map((r) => ({
    "Дата": fmtDate(r.date),
    "Материал": items.find((i) => i.id === r.itemId)?.name || "—",
    "Количество": r.qty,
    "Цена за единицу, ₽": Math.round(r.unitCost * 100) / 100,
    "Сумма закупки, ₽": r.totalCost,
    "Поставщик / комментарий": r.supplier || "",
  }));

  const objectRows = objects.map((o) => ({
    "Номер": o.number,
    "Название": o.name,
    "Статус": o.active ? "Активен" : "Выбыл",
  }));

  const purchaseOptionRows = purchaseOptions.map((o) => ({
    "Материал": items.find((i) => i.id === o.itemId)?.name || "—",
    "Поставщик / магазин": o.source,
    "Цена, ₽": o.price || "",
    "Ссылка": o.url || "",
    "Комментарий": o.note || "",
    "Дата добавления": fmtDate(o.date),
  }));

  downloadWorkbook(
    [
      { name: "Каталог", rows: itemRows },
      { name: "Журнал выдач", rows: issueRows },
      { name: "Журнал поступлений", rows: receiptRows },
      { name: "Объекты", rows: objectRows },
      { name: "Варианты закупки", rows: purchaseOptionRows },
    ],
    `sklad-merino-home-${todayISO()}.xlsx`
  );
};

// Выгрузка отчёта по затратам за выбранный период — сводка + разбивка по
// объектам и категориям, с учётом текущей наценки на логистику, плюс полная
// построчная детализация каждой выдачи.
const exportReport = (periodLabel, summary, byRoom, byCategory, detailedRows) => {
  const summaryRows = [
    { "Показатель": "Период", "Значение": periodLabel },
    { "Показатель": "Себестоимость выдач, ₽", "Значение": Math.round(summary.totalCost) },
    { "Показатель": `Логистика (${summary.markupPercent}%), ₽`, "Значение": Math.round(summary.markupAmount) },
    { "Показатель": "Итого к выставлению, ₽", "Значение": Math.round(summary.billedTotal) },
    { "Показатель": "Единиц выдано", "Значение": summary.totalQty },
    { "Показатель": "Объектов обслужено", "Значение": summary.roomsCount },
  ];

  const roomRows = byRoom.map((a) => ({
    "Номер": a.roomNumber,
    "Название": a.roomName,
    "Себестоимость, ₽": Math.round(a.cost),
    "Логистика, ₽": Math.round(a.logistics),
    "Итого к выставлению, ₽": Math.round(a.billed),
    "Единиц": a.qty,
    "Кол-во выдач": a.issues,
  }));

  const categoryRows = byCategory.map((c) => ({
    "Категория": c.cat,
    "Себестоимость, ₽": Math.round(c.cost),
  }));

  const detailRows = (detailedRows || []).map((d) => ({
    "Дата": fmtDate(d.date),
    "Объект (номер)": d.roomNumber,
    "Объект (название)": d.roomName,
    "Материал": d.itemName,
    "Количество": d.qty,
    "Себестоимость, ₽": Math.round(d.cost),
    "Логистика, ₽": Math.round(d.logistics),
    "Итого, ₽": Math.round(d.total),
  }));

  downloadWorkbook(
    [
      { name: "Сводка", rows: summaryRows },
      { name: "По объектам", rows: roomRows },
      { name: "По категориям", rows: categoryRows },
      { name: "Детализация", rows: detailRows },
    ],
    `otchet-${periodLabel.replace(/\s+/g, "-")}-${todayISO()}.xlsx`
  );
};

// Выгрузка общескладского инвентаря: текущее распределение по местам плюс
// полный журнал всех перемещений.
const exportAssets = (assets, transfers, objects, warehouses) => {
  const resolveLabel = (key) => {
    if (key === "written_off") return "Списано";
    const wh = warehouses.find((w) => w.id === key);
    if (wh) return wh.name;
    const obj = objects.find((o) => o.number === key);
    if (obj) return `${key} — ${obj.name}`;
    const t = transfers.find((t) => t.fromKey === key || t.toKey === key);
    if (t) return `${key} — ${t.fromKey === key ? t.fromLabel : t.toLabel}`;
    return key;
  };

  const distributionRows = [];
  assets.forEach((asset) => {
    const dist = getAssetDistribution(asset, transfers);
    Object.entries(dist)
      .filter(([, qty]) => qty > 0)
      .forEach(([key, qty]) => {
        distributionRows.push({
          "Инвентарь": asset.name,
          "Местоположение": resolveLabel(key),
          "Количество": qty,
        });
      });
  });

  const transferRows = transfers.map((t) => ({
    "Дата": fmtDate(t.date),
    "Инвентарь": assets.find((a) => a.id === t.assetId)?.name || "—",
    "Откуда": t.fromLabel,
    "Куда": t.toLabel,
    "Количество": t.quantity,
    "Комментарий": t.note || "",
  }));

  downloadWorkbook(
    [
      { name: "Текущее расположение", rows: distributionRows },
      { name: "Журнал перемещений", rows: transferRows },
    ],
    `inventar-${todayISO()}.xlsx`
  );
};

// ---------- роли доступа ----------
// Пароли здесь захардкожены прямо в коде приложения — а значит, любой, кто
// откроет исходный код артефакта или консоль разработчика, сможет их увидеть.
// Это НЕ настоящая защита данных, а лёгкий барьер на уровне интерфейса,
// чтобы случайный человек не переключился в чужую роль. Для реальной защиты
// нужна авторизация на сервере с проверкой пароля вне браузера пользователя.

const ROLES = {
  admin: { label: "Администратор", emoji: "🛠️", password: "admin2026", tabs: ["catalog", "issue", "assets", "report", "prices", "audit"], defaultTab: "catalog" },
  warehouse: { label: "Сотрудник склада", emoji: "📦", password: "sklad2026", tabs: ["issue", "assets"], defaultTab: "issue" },
  finance: { label: "Финансовый отдел", emoji: "📊", password: "finotdel2026", tabs: ["report", "prices"], defaultTab: "report" },
};

const TAB_META = {
  catalog: { label: "Каталог", emoji: "📦" },
  issue: { label: "Выдача", emoji: "📤" },
  assets: { label: "Инвентарь", emoji: "🛏️" },
  report: { label: "Отчёты", emoji: "📊" },
  prices: { label: "Цены", emoji: "📈" },
  audit: { label: "Журнал", emoji: "📝" },
};

// ---------- персистентное хранилище ----------
// Данные склада — общие для всех сотрудников (это не личный блокнот одного
// человека, а рабочее состояние всего склада), поэтому все правки видят все,
// и данные переживают обновления самого приложения.
//
// Хранилище — Supabase (бесплатный облачный сервис для хранения данных).
// Заполните эти две строки своими значениями: Project Settings → API →
// "Project URL" и "anon public" key на сайте supabase.com — после этого
// на настоящем сайте (Netlify и т.п.) данные будут сохраняться туда.
//
// Пока строки не заполнены (или это предпросмотр здесь, в чате с Claude, где
// библиотека Supabase недоступна) — приложение автоматически откатывается на
// временное хранилище предпросмотра, чтобы интерфейс не падал и его можно
// было тестировать. Ничего переключать вручную не нужно.
const SUPABASE_URL = "https://jvkvsadptflovcodbdev.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_fGPst_qCnmrGXQtFtBhJyw_1lmjwzOY";

const isSupabaseConfigured = SUPABASE_URL.startsWith("https://") && SUPABASE_URL.includes("supabase.co");
const hasPreviewStorage = typeof window !== "undefined" && !!window.storage;
const PREVIEW_STORAGE_KEY = "warehouse:state";

// Библиотеку Supabase подключаем динамически и только если реально заполнены
// ссылка и ключ. Если сама загрузка библиотеки не удалась (например, здесь,
// в предпросмотре Claude, где этой библиотеки физически нет) — тихо
// откатываемся на запасное хранилище вместо того, чтобы зависнуть или упасть.
// На настоящем сайте (после сборки через Vite/Netlify) она подключится нормально.
let supabaseClient = null;
let supabaseInitPromise = null;
const getSupabase = async () => {
  if (!isSupabaseConfigured) return null;
  if (supabaseClient) return supabaseClient;
  if (!supabaseInitPromise) {
    supabaseInitPromise = (async () => {
      try {
        const mod = await import("@supabase/supabase-js");
        supabaseClient = mod.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
        return supabaseClient;
      } catch (e) {
        console.warn("Supabase недоступен в этом окружении, используем запасное хранилище:", e);
        return null;
      }
    })();
  }
  return supabaseInitPromise;
};

// Всё состояние склада хранится ОДНОЙ записью — так одно действие
// пользователя даёт ровно одно обращение к хранилищу.
const loadState = async () => {
  const supabase = await getSupabase();
  if (supabase) {
    try {
      const { data, error } = await supabase
        .from("warehouse_state")
        .select("data")
        .eq("id", "main")
        .single();
      if (error || !data) return null; // строки ещё нет — это нормально при первом запуске
      return data.data;
    } catch {
      return null;
    }
  }
  if (hasPreviewStorage) {
    try {
      const res = await window.storage.get(PREVIEW_STORAGE_KEY, true);
      return res ? JSON.parse(res.value) : null;
    } catch {
      return null; // ключа ещё нет — это нормально при первом запуске
    }
  }
  return null;
};

// Сохраняет состояние в то хранилище, которое реально доступно (см. выше).
// Бросает исключение, если сохранить некуда — вызывающий код это ловит и
// показывает предупреждение вместо тихой потери данных.
const persistPayload = async (payload) => {
  const supabase = await getSupabase();
  if (supabase) {
    const { error } = await supabase
      .from("warehouse_state")
      .upsert({ id: "main", data: payload, updated_at: new Date().toISOString() });
    if (error) throw error;
    return;
  }
  if (hasPreviewStorage) {
    await window.storage.set(PREVIEW_STORAGE_KEY, JSON.stringify(payload), true);
    return;
  }
  throw new Error("Хранилище не настроено — заполните SUPABASE_URL и SUPABASE_ANON_KEY.");
};

// ---------- root ----------

export default function WarehouseApp() {
  const [items, setItems] = useState(seedItems);
  const [issues, setIssues] = useState(seedIssues);
  const [receipts, setReceipts] = useState(seedReceipts);
  const [objects, setObjects] = useState(seedObjects);
  const [assets, setAssets] = useState(seedAssets);
  const [assetTransfers, setAssetTransfers] = useState(seedAssetTransfers);
  const [warehouses, setWarehouses] = useState(seedWarehouses);
  const [changeLog, setChangeLog] = useState([]);
  const [purchaseOptions, setPurchaseOptions] = useState(seedPurchaseOptions);
  const [role, setRole] = useState(null); // null = не авторизован, показываем экран входа
  const [tab, setTab] = useState("catalog");
  // Фильтр каталога — включается кликом по wallet-карточкам ("Товарных
  // позиций" → catalogFilter = "inStock", "Нужно пополнить" → "lowStock"),
  // сбрасывается вручную (крестиком) или при обычном переходе по навигации.
  const [catalogFilter, setCatalogFilter] = useState(null); // null | "inStock" | "lowStock"
  // Наценка на логистику поверх реальной себестоимости расходников.
  // Хранится как доля (0.2 = 20%) и применяется только к стоимости ВЫДАННЫХ
  // материалов в отчётах — так видно и себестоимость, и сумму к учёту, и саму маржу.
  const [markupRate, setMarkupRate] = useState(0.3);
  const [dataLoaded, setDataLoaded] = useState(false);
  const [saveError, setSaveError] = useState(false);

  // Стили вставляем не через <style>{CSS}</style> в разметке, а императивно
  // в document.head — и первым делом удаляем ЛЮБЫЕ старые версии этого тега
  // (по id), какие бы ни остались от предыдущей загрузки в этом же превью.
  // Так исключается ситуация, когда старый набор правил (например, старая
  // раскладка формы) продолжает действовать поверх нового просто потому что
  // его тег оказался в документе позже нового по случайному порядку.
  useEffect(() => {
    try {
      document.querySelectorAll("style[data-wh-app-style]").forEach((el) => el.remove());
      const styleEl = document.createElement("style");
      styleEl.setAttribute("data-wh-app-style", "true");
      styleEl.textContent = CSS;
      document.head.appendChild(styleEl);
    } catch {
      // нет доступа к document — просто пропускаем, JSX-вариант ниже
      // на такой случай пока оставлен как запасной
    }
  }, []);

  // Иконка приложения — favicon вкладки браузера и заголовок страницы.
  // Выполняется один раз при монтировании; безопасно (просто ничего не
  // сделает), если компонент рендерится вне обычного document (например,
  // в некоторых предпросмотрах без доступа к head).
  useEffect(() => {
    try {
      let link = document.querySelector("link[rel~='icon']");
      if (!link) {
        link = document.createElement("link");
        link.rel = "icon";
        document.head.appendChild(link);
      }
      link.href = APP_ICON;
      document.title = "Склад Merino Home";
    } catch {
      // нет доступа к document — просто пропускаем
    }
  }, []);

  // Сохраняет ПОЛНОЕ состояние склада одним запросом. Принимает только те поля,
  // которые реально поменялись, остальные берёт из текущего состояния —
  // так каждое действие пользователя даёт ровно одну запись в хранилище.
  //
  // Важно: перед записью сначала подтягиваем САМОЕ СВЕЖЕЕ состояние из базы
  // и берём поля, которые сейчас не меняются, именно из него, а не из
  // локальной памяти этой вкладки. Раньше если сайт был открыт в двух
  // вкладках/на двух устройствах одновременно, вкладка, которая простояла
  // открытой дольше, могла своим сохранением затереть то, что успели
  // добавить в другом месте — просто потому что "помнила" более старую
  // версию данных. Так это исключается для всех полей, кроме тех, что
  // реально меняет именно это действие.
  const saveState = (partial) => {
    (async () => {
      let fresh = null;
      try {
        fresh = await loadState();
      } catch {
        fresh = null;
      }
      const base = fresh || { items, issues, receipts, objects, assets, assetTransfers, warehouses, changeLog, purchaseOptions, markupRate };
      const payload = {
        items: partial.items ?? base.items ?? items,
        issues: partial.issues ?? base.issues ?? issues,
        receipts: partial.receipts ?? base.receipts ?? receipts,
        objects: partial.objects ?? base.objects ?? objects,
        assets: partial.assets ?? base.assets ?? assets,
        assetTransfers: partial.assetTransfers ?? base.assetTransfers ?? assetTransfers,
        warehouses: partial.warehouses ?? base.warehouses ?? warehouses,
        changeLog: partial.changeLog ?? base.changeLog ?? changeLog,
        purchaseOptions: partial.purchaseOptions ?? base.purchaseOptions ?? purchaseOptions,
        markupRate: partial.markupRate ?? base.markupRate ?? markupRate,
      };
      await persistPayload(payload);
      // Если в базе оказались более свежие данные по полям, которые эта
      // вкладка не меняла сейчас, — подтягиваем их и в её собственное
      // состояние, чтобы интерфейс не расходился с тем, что реально сохранено.
      if (fresh) {
        if (partial.items === undefined) setItems(payload.items);
        if (partial.issues === undefined) setIssues(payload.issues);
        if (partial.receipts === undefined) setReceipts(payload.receipts);
        if (partial.objects === undefined) setObjects(payload.objects);
        if (partial.assets === undefined) setAssets(payload.assets);
        if (partial.assetTransfers === undefined) setAssetTransfers(payload.assetTransfers);
        if (partial.warehouses === undefined) setWarehouses(payload.warehouses);
        if (partial.changeLog === undefined) setChangeLog(payload.changeLog);
        if (partial.purchaseOptions === undefined) setPurchaseOptions(payload.purchaseOptions);
        if (partial.markupRate === undefined) setMarkupRate(payload.markupRate);
      }
    })()
      .then(() => setSaveError(false))
      .catch((e) => {
        console.error("Ошибка сохранения:", e);
        setSaveError(true);
      });
  };

  // Добавляет запись в журнал изменений и сразу сохраняет — используется всеми
  // административными действиями (правка/удаление позиций, объекты, инвентарь,
  // склады, наценка), которые не попадают ни в журнал выдач, ни в историю
  // поступлений, ни в журнал перемещений инвентаря. Необязательный restorable —
  // снимок удалённых данных ({ type, data }), чтобы запись можно было вернуть
  // прямо из журнала.
  const logChange = (action, details, extra = {}, restorable = null) => {
    const entry = { id: nextId("log"), date: nowStamp(), role, action, details, restorable };
    const newLog = [entry, ...changeLog];
    setChangeLog(newLog);
    saveState({ changeLog: newLog, ...extra });
  };

  // Подтягиваем сохранённое состояние склада при каждом запуске приложения —
  // это должно отработать один раз при монтировании, до любых действий
  // пользователя, поэтому хук вызывается безусловно и раньше всех return.
  useEffect(() => {
    let cancelled = false;
    (async () => {
      const loaded = await loadState();
      if (cancelled) return;

      const finalItems = loaded?.items ?? seedItems;
      const finalIssues = loaded?.issues ?? seedIssues;
      const finalReceipts = loaded?.receipts ?? seedReceipts;
      const finalObjects = loaded?.objects ?? seedObjects;
      const finalAssets = loaded?.assets ?? seedAssets;
      const finalAssetTransfers = loaded?.assetTransfers ?? seedAssetTransfers;
      const finalWarehouses = loaded?.warehouses ?? seedWarehouses;
      const finalChangeLog = loaded?.changeLog ?? [];
      const finalPurchaseOptions = loaded?.purchaseOptions ?? seedPurchaseOptions;
      const finalRate = loaded?.markupRate ?? 0.3;

      setItems(finalItems);
      setIssues(finalIssues);
      setReceipts(finalReceipts);
      setObjects(finalObjects);
      setAssets(finalAssets);
      setAssetTransfers(finalAssetTransfers);
      setWarehouses(finalWarehouses);
      setChangeLog(finalChangeLog);
      setPurchaseOptions(finalPurchaseOptions);
      setMarkupRate(finalRate);

      // Если в базе ещё ничего не было (самый первый запуск) — сразу
      // сохраняем стартовые данные одной записью.
      if (loaded == null) {
        persistPayload({
          items: finalItems, issues: finalIssues, receipts: finalReceipts, objects: finalObjects,
          assets: finalAssets, assetTransfers: finalAssetTransfers, warehouses: finalWarehouses,
          changeLog: finalChangeLog, purchaseOptions: finalPurchaseOptions, markupRate: finalRate,
        }).catch((e) => console.error("Ошибка первичного сохранения:", e));
      }

      setDataLoaded(true);
    })();
    return () => { cancelled = true; };
  }, []);

  const allowedTabs = role ? ROLES[role].tabs : [];

  const logIn = (nextRole) => {
    setRole(nextRole);
    setTab(ROLES[nextRole].defaultTab);
  };

  const logOut = () => setRole(null);

  // Переход по вкладке с проверкой прав — так клики по wallet-карточкам
  // не пытаются открыть раздел, недоступный текущей роли.
  const goTo = (nextTab) => {
    if (allowedTabs.includes(nextTab)) setTab(nextTab);
  };

  const goToCatalogFiltered = (filterMode) => {
    if (!allowedTabs.includes("catalog")) return;
    setCatalogFilter(filterMode);
    setTab("catalog");
  };

  const lowStockCount = items.filter((i) => i.quantity <= i.minQuantity).length;
  const totalValue = items.reduce((s, i) => s + i.quantity * i.unitCost, 0);

  if (!dataLoaded) {
    return (
      <div className="wh-root wh-login-root">
        <div className="wh-login-card">
          <div className="wh-login-logo"><img src={MERINO_LOGO} alt="Merino Home" /></div>
          <div className="wh-login-title">Склад Merino Home</div>
          <div className="wh-login-sub">Загружаем данные склада…</div>
        </div>
      </div>
    );
  }

  if (!role) {
    return <LoginGate onLogIn={logIn} />;
  }

  const issueItem = (itemId, roomNumber, roomName, qty, note) => {
    const item = items.find((i) => i.id === itemId);
    if (!item) return;
    const q = Math.min(qty, item.quantity);
    if (q <= 0) return;
    const newItems = items.map((i) => (i.id === itemId ? { ...i, quantity: i.quantity - q } : i));
    const newIssue = {
      id: nextId("s"),
      itemId,
      roomNumber,
      roomName,
      qty: q,
      date: todayISO(),
      note: note.trim(),
      cost: q * item.unitCost,
    };
    const newIssues = [newIssue, ...issues];
    setItems(newItems);
    setIssues(newIssues);
    saveState({ items: newItems, issues: newIssues });
  };

  // Оприходовать партию для уже существующей позиции: количество и себестоимость
  // пересчитываются как средневзвешенные между остатком и новой закупкой.
  const receiveStock = (itemId, qty, totalCost, supplier) => {
    if (qty <= 0) return;
    const newItems = items.map((i) => {
      if (i.id !== itemId) return i;
      const newQty = i.quantity + qty;
      const newCost = newQty > 0 ? (i.quantity * i.unitCost + totalCost) / newQty : i.unitCost;
      return { ...i, quantity: newQty, unitCost: newCost };
    });
    const newReceipt = { id: nextId("r"), itemId, qty, unitCost: totalCost / qty, totalCost, date: todayISO(), supplier: supplier.trim() };
    const newReceipts = [newReceipt, ...receipts];
    setItems(newItems);
    setReceipts(newReceipts);
    saveState({ items: newItems, receipts: newReceipts });
  };

  // Приход новой позиции, отсутствующей в каталоге: сразу считаем итоговое
  // количество и себестоимость первой партии одним шагом (без промежуточного
  // состояния с нулевым остатком), чтобы не терять данные при сохранении.
  const receiveNewItem = (data, qty, totalCost, supplier) => {
    const id = nextId("i");
    const newItem = { id, ...data, quantity: qty, unitCost: qty > 0 ? totalCost / qty : 0 };
    const newItems = [newItem, ...items];
    const newReceipt = { id: nextId("r"), itemId: id, qty, unitCost: totalCost / qty, totalCost, date: todayISO(), supplier: supplier.trim() };
    const newReceipts = [newReceipt, ...receipts];
    setItems(newItems);
    setReceipts(newReceipts);
    saveState({ items: newItems, receipts: newReceipts });
  };

  // Управление реестром объектов: добавление нового и мягкое включение/
  // выключение (объекты не удаляются физически, чтобы не потерять ссылки
  // из уже сделанных выдач и отчётов по ним).
  const addObject = (number, name) => {
    const newObjects = [...objects, { id: nextId("o"), number: number.trim(), name: name.trim(), active: true }];
    setObjects(newObjects);
    logChange("Добавлен объект", `${number.trim()} — ${name.trim()}`, { objects: newObjects });
  };

  const toggleObjectActive = (id) => {
    const obj = objects.find((o) => o.id === id);
    const newObjects = objects.map((o) => (o.id === id ? { ...o, active: !o.active } : o));
    setObjects(newObjects);
    logChange(
      obj?.active ? "Объект отмечен выбывшим" : "Объект возвращён в активные",
      `${obj?.number} — ${obj?.name}`,
      { objects: newObjects }
    );
  };

  const removeItem = (id) => {
    const item = items.find((i) => i.id === id);
    const newItems = items.filter((i) => i.id !== id);
    setItems(newItems);
    logChange(
      "Удалена позиция каталога",
      `«${item?.name || id}»`,
      { items: newItems },
      item ? { type: "item", data: item } : null
    );
  };

  // Возвращает ранее удалённую позицию обратно в каталог — с тем же id,
  // что и раньше, чтобы старые поступления/выдачи по ней снова "увидели"
  // название товара, а не показывали "—".
  const restoreItem = (itemData) => {
    if (items.some((i) => i.id === itemData.id)) return; // уже есть, не дублируем
    const newItems = [itemData, ...items];
    setItems(newItems);
    logChange("Восстановлена позиция каталога", `«${itemData.name}»`, { items: newItems });
  };

  // Разовое восстановление 7 позиций категории "Средства для уборки",
  // удалённых ещё до того, как в журнале изменений появилась возможность
  // восстановления по кнопке (тем записям не за что "зацепиться" — снимок
  // данных на момент удаления тогда не сохранялся). Добавляет только те
  // позиции, которых сейчас нет в каталоге, по названию.
  const LEGACY_DELETED_ITEMS = [
    { code: "СУ-006", name: "Антижир / средство для кухни", category: "Средства для уборки", unit: "100 мл", minQuantity: 12 },
    { code: "СУ-007", name: "Средство от известкового налёта", category: "Средства для уборки", unit: "100 мл", minQuantity: 12 },
    { code: "СУ-008", name: "Дезинфицирующее средство", category: "Средства для уборки", unit: "100 мл", minQuantity: 10 },
    { code: "СУ-009", name: "Меламиновые губки", category: "Средства для уборки", unit: "шт", minQuantity: 20 },
    { code: "СУ-010", name: "Губки хозяйственные", category: "Средства для уборки", unit: "шт", minQuantity: 30 },
    { code: "СУ-011", name: "Перчатки одноразовые", category: "Средства для уборки", unit: "пара", minQuantity: 12 },
    { code: "СУ-012", name: "Тряпки / салфетки для уборки", category: "Средства для уборки", unit: "шт", minQuantity: 25 },
  ];
  const restoreLegacyDeletedItems = () => {
    const missing = LEGACY_DELETED_ITEMS.filter(
      (li) => !items.some((i) => i.name === li.name)
    );
    if (missing.length === 0) return;
    const restored = missing.map((li) => ({ id: nextId("i"), ...li, quantity: 0, unitCost: 0 }));
    const newItems = [...restored, ...items];
    setItems(newItems);
    logChange("Восстановлены позиции каталога (пакетно)", `${restored.map((r) => r.name).join(", ")}`, { items: newItems });
  };

  // Справочник вариантов закупки по позиции — где смотрели, почём и по какой
  // ссылке, ещё до фактической покупки. Отдельно от "Истории поступлений",
  // где фиксируются уже совершённые закупки.
  const addPurchaseOption = (itemId, data) => {
    const newOption = {
      id: nextId("po"),
      itemId,
      source: data.source.trim(),
      url: data.url.trim(),
      price: data.price,
      note: data.note.trim(),
      date: todayISO(),
    };
    const newOptions = [newOption, ...purchaseOptions];
    setPurchaseOptions(newOptions);
    saveState({ purchaseOptions: newOptions });
  };

  const removePurchaseOption = (id) => {
    const newOptions = purchaseOptions.filter((o) => o.id !== id);
    setPurchaseOptions(newOptions);
    saveState({ purchaseOptions: newOptions });
  };

  const adjustQuantity = (id, delta) => {
    const newItems = items.map((i) => (i.id === id ? { ...i, quantity: Math.max(0, i.quantity + delta) } : i));
    setItems(newItems);
    saveState({ items: newItems });
  };

  // Редактирование карточки товара: только название/категория/единица/мин.
  // запас. Остаток и себестоимость этим путём не трогаем — они меняются
  // только через выдачу и приёмку партий, чтобы история расчётов не рвалась.
  const FIELD_LABELS = { name: "Название", category: "Категория", unit: "Ед. изм.", minQuantity: "Мин. запас" };
  const editItem = (id, updates) => {
    const oldItem = items.find((i) => i.id === id);
    const newItems = items.map((i) => (i.id === id ? { ...i, ...updates } : i));
    setItems(newItems);
    const changes = Object.entries(updates)
      .filter(([k, v]) => oldItem && oldItem[k] !== v)
      .map(([k, v]) => `${FIELD_LABELS[k] || k}: «${oldItem[k]}» → «${v}»`)
      .join("; ");
    logChange("Изменена позиция каталога", `«${oldItem?.name}» — ${changes || "без изменений"}`, { items: newItems });
  };

  const changeMarkupRate = (rate) => {
    setMarkupRate(rate);
    logChange("Изменена наценка на логистику", `${Math.round(rate * 100)}%`, { markupRate: rate });
  };

  // Полная очистка журнала выдач — для тестовых/демонстрационных записей,
  // которые не должны попасть в реальную историю и отчёты. Остатки и
  // себестоимость товаров этим не трогаются, очищается только сам журнал.
  const clearIssues = () => {
    setIssues([]);
    logChange("Очищен журнал выдач", `Удалено записей: ${issues.length}`, { issues: [] });
  };

  // Инвентарь: новый тип оборудования — весь стартовый объём считается
  // лежащим на складе (никаких перемещений ещё не было).
  const addAssetType = (name, totalQuantity) => {
    const newAssets = [...assets, { id: nextId("a"), name: name.trim(), totalQuantity }];
    setAssets(newAssets);
    logChange("Добавлен тип инвентаря", `«${name.trim()}», ${totalQuantity} шт`, { assets: newAssets });
  };

  // Пополнение уже существующего типа (закупили ещё раскладушек) — прирост
  // всегда попадает на склад, откуда его дальше можно куда-то переместить.
  const restockAsset = (id, addQuantity) => {
    const asset = assets.find((a) => a.id === id);
    const newAssets = assets.map((a) => (a.id === id ? { ...a, totalQuantity: a.totalQuantity + addQuantity } : a));
    setAssets(newAssets);
    logChange("Пополнение инвентаря", `«${asset?.name}»: +${addQuantity} шт`, { assets: newAssets });
  };

  // Удаление типа инвентаря целиком (например, завели по ошибке). Журнал
  // перемещений не трогаем — как и с расходниками, прошлые записи остаются,
  // просто название инвентаря в них перестанет находиться по id.
  const removeAssetType = (id) => {
    const asset = assets.find((a) => a.id === id);
    const newAssets = assets.filter((a) => a.id !== id);
    setAssets(newAssets);
    logChange("Удалён тип инвентаря", `«${asset?.name || id}»`, { assets: newAssets });
  };

  // Добавление ещё одного склада как локации для перемещения инвентаря
  // (например, открылся третий склад). Новый склад стартует пустым — всё,
  // что в него нужно, перемещается с других складов через обычный перевод.
  const addWarehouse = (name) => {
    const newWarehouses = [...warehouses, { id: nextId("wh"), name: name.trim() }];
    setWarehouses(newWarehouses);
    logChange("Добавлен склад", name.trim(), { warehouses: newWarehouses });
  };

  // Перемещение инвентаря между складом и объектами (в любую сторону) —
  // фиксируем как запись в журнале, текущее местоположение считается из него.
  // Списание (сломался/утерян) — это тоже перемещение, только в специальное
  // место "written_off": totalQuantity не трогаем, но эти единицы больше не
  // показываются как доступные ни в одной обычной локации.
  const transferAsset = (assetId, fromKey, fromLabel, toKey, toLabel, quantity, note) => {
    if (quantity <= 0 || fromKey === toKey) return;
    const newTransfer = {
      id: nextId("at"),
      assetId,
      fromKey,
      fromLabel,
      toKey,
      toLabel,
      quantity,
      date: todayISO(),
      note: note.trim(),
    };
    const newTransfers = [newTransfer, ...assetTransfers];
    setAssetTransfers(newTransfers);
    saveState({ assetTransfers: newTransfers });
  };

  const totalIssuesCost = issues.reduce((s, x) => s + x.cost, 0);

  return (
    <div className="wh-root">
      <div className="wh-page">

        {saveError && (
          <div className="wh-save-error">
            <AlertTriangle size={15} />
            Не удалось сохранить последнее изменение в хранилище — проверьте соединение и повторите действие.
            Если ошибка повторяется, лучше сразу выгрузить данные в Excel, чтобы не потерять их.
          </div>
        )}

        <div className="wh-hero-sheet">
          <div className="wh-hero-nav">
            <div className="wh-brand">
              <div className="wh-brand-badge"><img src={MERINO_LOGO} alt="Merino Home" /></div>
              <div>
                <div className="wh-brand-title">Склад Merino Home</div>
                <div className="wh-brand-sub">Хозобслуживание номеров</div>
              </div>
            </div>

            <div className="wh-tab-pill-wrap">
              <nav className="wh-tab-pill">
                {allowedTabs.map((t) => (
                  <button key={t} className={tab === t ? "active" : ""} onClick={() => { setCatalogFilter(null); setTab(t); }}>
                    {TAB_META[t].emoji} {TAB_META[t].label}
                  </button>
                ))}
              </nav>
            </div>

            <div className="wh-role-badge">
              <span>{ROLES[role].emoji} {ROLES[role].label}</span>
              <button className="wh-logout-btn" onClick={logOut}>Выйти</button>
            </div>
          </div>
        </div>

        {role === "admin" && (
          <div className="wh-wallet-row">
            <button className="wh-wallet-card orange" onClick={() => goToCatalogFiltered("inStock")}>
              <div className="wh-wallet-icon">📦</div>
              <div className="w-label">Товарных позиций</div>
              <div className="w-value">{num(items.length)}</div>
              <div className="w-sub">в {CATEGORIES.length} категориях</div>
            </button>
            <button className="wh-wallet-card blue" onClick={() => goTo("catalog")}>
              <div className="wh-wallet-icon">💰</div>
              <div className="w-label">Стоимость остатков</div>
              <div className="w-value">{rub(totalValue)}</div>
              <div className="w-sub">по текущей себестоимости</div>
            </button>
            <button className="wh-wallet-card cyan" onClick={() => goToCatalogFiltered("lowStock")}>
              <div className="wh-wallet-icon">⚠️</div>
              <div className="w-label">Нужно пополнить</div>
              <div className="w-value">{num(lowStockCount)}</div>
              <div className="w-sub">позиций ниже минимума</div>
            </button>
            <button className="wh-wallet-card green" onClick={() => goTo("report")}>
              <div className="wh-wallet-icon">📤</div>
              <div className="w-label">Выдано материалов</div>
              <div className="w-value">{rub(totalIssuesCost)}</div>
              <div className="w-sub">за всё время</div>
            </button>
          </div>
        )}

        <main className="wh-main">
          {role === "admin" && tab === "catalog" && (
            <CatalogView
              items={items}
              issues={issues}
              receipts={receipts}
              objects={objects}
              onRemove={removeItem}
              onAdjust={adjustQuantity}
              onEdit={editItem}
              onReceiveStock={receiveStock}
              onReceiveNewItem={receiveNewItem}
              stockFilter={catalogFilter}
              onClearStockFilter={() => setCatalogFilter(null)}
              purchaseOptions={purchaseOptions}
              onAddPurchaseOption={addPurchaseOption}
              onRemovePurchaseOption={removePurchaseOption}
            />
          )}
          {(role === "admin" || role === "warehouse") && tab === "issue" && (
            <IssueView
              items={items}
              issues={issues}
              objects={objects}
              role={role}
              onIssue={issueItem}
              onAddObject={addObject}
              onToggleObjectActive={toggleObjectActive}
              onClearIssues={clearIssues}
            />
          )}
          {(role === "admin" || role === "warehouse") && tab === "assets" && (
            <AssetsView
              assets={assets}
              transfers={assetTransfers}
              objects={objects}
              warehouses={warehouses}
              role={role}
              onAddAssetType={addAssetType}
              onRestock={restockAsset}
              onRemoveAssetType={removeAssetType}
              onTransfer={transferAsset}
              onAddWarehouse={addWarehouse}
            />
          )}
          {(role === "admin" || role === "finance") && tab === "report" && (
            <ReportView items={items} issues={issues} role={role} markupRate={markupRate} onMarkupChange={changeMarkupRate} />
          )}
          {(role === "admin" || role === "finance") && tab === "prices" && (
            <PriceTrendsView items={items} receipts={receipts} />
          )}
          {role === "admin" && tab === "audit" && (
            <AuditLogView
              log={changeLog}
              items={items}
              onRestoreItem={restoreItem}
              onRestoreLegacy={restoreLegacyDeletedItems}
            />
          )}
        </main>
      </div>
    </div>
  );
}

// ---------- Login ----------

function LoginGate({ onLogIn }) {
  const [roleChoice, setRoleChoice] = useState("admin");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");

  const submit = () => {
    if (password.trim() === ROLES[roleChoice].password) {
      setError("");
      onLogIn(roleChoice);
    } else {
      setError("Неверный пароль. Проверьте раскладку клавиатуры и повторите попытку.");
    }
  };

  return (
    <div className="wh-root wh-login-root">
      <div className="wh-login-card">
        <div className="wh-login-logo"><img src={MERINO_LOGO} alt="Merino Home" /></div>
        <div className="wh-login-title">Склад Merino Home</div>
        <div className="wh-login-sub">Войдите, чтобы продолжить</div>

        <label className="wh-login-field">
          Роль
          <select value={roleChoice} onChange={(e) => { setRoleChoice(e.target.value); setError(""); }}>
            {Object.entries(ROLES).map(([key, r]) => (
              <option key={key} value={key}>{r.emoji} {r.label}</option>
            ))}
          </select>
        </label>

        <label className="wh-login-field">
          Пароль
          <input
            type="password"
            value={password}
            onChange={(e) => { setPassword(e.target.value); setError(""); }}
            onKeyDown={(e) => { if (e.key === "Enter") submit(); }}
            placeholder="Введите пароль"
            autoFocus
          />
        </label>

        {error && <div className="wh-login-error">{error}</div>}

        <button type="button" className="wh-btn-primary wh-login-submit" onClick={submit}>Войти</button>
      </div>
    </div>
  );
}

// ---------- Catalog ----------

function CatalogView({ items, issues, receipts, objects, onRemove, onAdjust, onEdit, onReceiveStock, onReceiveNewItem, stockFilter, onClearStockFilter, purchaseOptions, onAddPurchaseOption, onRemovePurchaseOption }) {
  const [query, setQuery] = useState("");
  const [openForm, setOpenForm] = useState(null); // null | "new" | "receive"
  const [collapsed, setCollapsed] = useState({});
  const [editingId, setEditingId] = useState(null);
  const [receiptFilter, setReceiptFilter] = useState("");
  const [linksForId, setLinksForId] = useState(null);
  const [newLinkSource, setNewLinkSource] = useState("");
  const [newLinkUrl, setNewLinkUrl] = useState("");
  const [newLinkPrice, setNewLinkPrice] = useState("");
  const [newLinkNote, setNewLinkNote] = useState("");

  const submitPurchaseOption = (itemId) => {
    if (!newLinkSource.trim()) return;
    onAddPurchaseOption(itemId, {
      source: newLinkSource,
      url: newLinkUrl,
      price: Number(newLinkPrice) || 0,
      note: newLinkNote,
    });
    setNewLinkSource("");
    setNewLinkUrl("");
    setNewLinkPrice("");
    setNewLinkNote("");
  };

  const filtered = items.filter((i) => {
    const matchesQuery = (i.name + i.category).toLowerCase().includes(query.toLowerCase());
    const matchesFilter =
      stockFilter === "inStock" ? i.quantity > 0 :
      stockFilter === "lowStock" ? i.quantity <= i.minQuantity :
      true;
    return matchesQuery && matchesFilter;
  });

  const byCategory = CATEGORIES.map((cat) => ({
    cat,
    list: filtered.filter((i) => i.category === cat),
  })).filter((g) => g.list.length > 0);

  const filteredReceipts = receipts.filter((r) => {
    const itemName = items.find((i) => i.id === r.itemId)?.name || "";
    const q = receiptFilter.toLowerCase();
    return itemName.toLowerCase().includes(q) || (r.supplier || "").toLowerCase().includes(q);
  });

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>Каталог расходников</h1>
          <p className="wh-sub">Остатки, себестоимость и минимальный запас по каждой позиции</p>
        </div>
        <div className="wh-header-actions">
          <button
            className="wh-btn-icon-export"
            onClick={() => exportAllData(items, issues, receipts, objects, purchaseOptions)}
            title="Выгрузить в Excel"
          >
            <FileSpreadsheet size={18} />
          </button>
          <button
            className="wh-btn-secondary"
            onClick={() => setOpenForm((v) => (v === "receive" ? null : "receive"))}
          >
            <PackagePlus size={16} /> Оприходовать партию
          </button>
          <button
            className="wh-btn-primary"
            onClick={() => setOpenForm((v) => (v === "new" ? null : "new"))}
          >
            <Plus size={16} /> Добавить позицию
          </button>
        </div>
      </div>

      <div className="wh-search">
        <Search size={16} />
        <input
          placeholder="Найти по названию, коду или категории…"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
        />
      </div>

      {stockFilter === "inStock" && (
        <div className="wh-filter-chip">
          <Package size={13} /> Только позиции в наличии
          <button onClick={onClearStockFilter} title="Убрать фильтр"><X size={13} /></button>
        </div>
      )}
      {stockFilter === "lowStock" && (
        <div className="wh-filter-chip warn">
          <AlertTriangle size={13} /> Только то, что нужно пополнить
          <button onClick={onClearStockFilter} title="Убрать фильтр"><X size={13} /></button>
        </div>
      )}

      {openForm === "new" && (
        <ReceiveForm
          items={items}
          receipts={receipts}
          purchaseOptions={purchaseOptions}
          initialMode="new"
          onReceiveStock={onReceiveStock}
          onReceiveNewItem={onReceiveNewItem}
          onClose={() => setOpenForm(null)}
        />
      )}
      {openForm === "receive" && (
        <ReceiveForm
          items={items}
          receipts={receipts}
          purchaseOptions={purchaseOptions}
          initialMode="existing"
          onReceiveStock={onReceiveStock}
          onReceiveNewItem={onReceiveNewItem}
          onClose={() => setOpenForm(null)}
        />
      )}

      {editingId && (
        <EditItemForm
          item={items.find((i) => i.id === editingId)}
          onSave={(updates) => { onEdit(editingId, updates); setEditingId(null); }}
          onClose={() => setEditingId(null)}
        />
      )}

      {byCategory.map(({ cat, list }) => {
        const isCollapsed = collapsed[cat];
        const catValue = list.reduce((s, i) => s + i.quantity * i.unitCost, 0);
        return (
          <section key={cat} className="wh-cat-block">
            <button
              className="wh-cat-header"
              onClick={() => setCollapsed((c) => ({ ...c, [cat]: !c[cat] }))}
            >
              {isCollapsed ? <ChevronRight size={16} /> : <ChevronDown size={16} />}
              <span className="wh-cat-name">{CATEGORY_EMOJI[cat] || "📦"} {cat}</span>
              <span className="wh-cat-count">{list.length}</span>
              <span className="wh-cat-value">{rub(catValue)}</span>
            </button>

            {!isCollapsed && (
              <div className="wh-table">
                <div className="wh-row wh-row-head">
                  <span className="wh-col-name">Наименование</span>
                  <span>Остаток</span>
                  <span>Мин.</span>
                  <span>Цена</span>
                  <span>Сумма</span>
                  <span></span>
                </div>
                {list.map((item) => {
                  const low = item.quantity <= item.minQuantity;
                  const itemOptions = purchaseOptions.filter((o) => o.itemId === item.id);
                  const linksOpen = linksForId === item.id;
                  return (
                    <React.Fragment key={item.id}>
                      <div className={`wh-row ${low ? "wh-row-low" : ""}`}>
                        <span className="wh-col-name">{item.name}</span>
                        <span className="wh-qty-cell">
                          <button className="wh-qty-btn" onClick={() => onAdjust(item.id, -1)}>−</button>
                          <span className="wh-mono">
                            {fmtQty(item.quantity, item.unit)}
                          </span>
                          <button className="wh-qty-btn" onClick={() => onAdjust(item.id, 1)}>+</button>
                        </span>
                        <span className="wh-mono wh-muted">{num(item.minQuantity)}</span>
                        <span className="wh-mono">{rub(item.unitCost)}</span>
                        <span className="wh-mono wh-strong">{rub(item.quantity * item.unitCost)}</span>
                        <span className="wh-row-actions">
                          {low && (
                            <span className="wh-badge-low" title="Ниже минимального запаса">
                              <AlertTriangle size={13} /> заказать
                            </span>
                          )}
                          <button
                            className={`wh-icon-btn ${itemOptions.length > 0 ? "wh-icon-btn-active" : ""}`}
                            onClick={() => {
                              setLinksForId(linksOpen ? null : item.id);
                              setNewLinkSource("");
                              setNewLinkUrl("");
                              setNewLinkPrice("");
                              setNewLinkNote("");
                            }}
                            title="Варианты закупки (ссылки и цены)"
                          >
                            🔗{itemOptions.length > 0 && <span className="wh-link-count">{itemOptions.length}</span>}
                          </button>
                          <button className="wh-icon-btn" onClick={() => setEditingId(item.id)} title="Редактировать позицию">
                            <Pencil size={14} />
                          </button>
                          <button className="wh-icon-btn" onClick={() => onRemove(item.id)} title="Удалить позицию">
                            <Trash2 size={14} />
                          </button>
                        </span>
                      </div>

                      {linksOpen && (
                        <div className="wh-purchase-links">
                          {itemOptions.length > 0 && (
                            <div className="wh-purchase-links-list">
                              {itemOptions.map((o) => (
                                <div className="wh-purchase-link-row" key={o.id}>
                                  <span className="wh-col-name">
                                    {o.url ? (
                                      <a href={o.url} target="_blank" rel="noopener noreferrer">{o.source}</a>
                                    ) : o.source}
                                  </span>
                                  <span className="wh-mono wh-strong">{o.price ? rub(o.price) : "—"}</span>
                                  <span className="wh-col-name wh-muted">{o.note || "—"}</span>
                                  <span className="wh-mono wh-muted">{fmtDate(o.date)}</span>
                                  <button className="wh-icon-btn" onClick={() => onRemovePurchaseOption(o.id)} title="Удалить вариант">
                                    <X size={13} />
                                  </button>
                                </div>
                              ))}
                            </div>
                          )}
                          <div className="wh-purchase-link-form">
                            <input placeholder="Магазин / поставщик" value={newLinkSource} onChange={(e) => setNewLinkSource(e.target.value)} />
                            <input placeholder="Ссылка (необязательно)" value={newLinkUrl} onChange={(e) => setNewLinkUrl(e.target.value)} />
                            <input type="number" min="0" placeholder="Цена, ₽" value={newLinkPrice} onChange={(e) => setNewLinkPrice(e.target.value)} />
                            <input placeholder="Комментарий" value={newLinkNote} onChange={(e) => setNewLinkNote(e.target.value)} />
                            <button className="wh-btn-primary" onClick={() => submitPurchaseOption(item.id)}>Добавить</button>
                          </div>
                        </div>
                      )}
                    </React.Fragment>
                  );
                })}
              </div>
            )}
          </section>
        );
      })}

      {byCategory.length === 0 && (
        <div className="wh-empty">Ничего не найдено по запросу «{query}»</div>
      )}

      {receipts.length > 0 && (
        <>
          <div className="wh-log-header">
            <h2>📥 История поступлений</h2>
            <div className="wh-search wh-search-small">
              <Search size={14} />
              <input
                placeholder="Фильтр по материалу или поставщику"
                value={receiptFilter}
                onChange={(e) => setReceiptFilter(e.target.value)}
              />
            </div>
          </div>
          <div className="wh-table">
            <div className="wh-row wh-row-head wh-row-receipt">
              <span>Дата</span>
              <span className="wh-col-name">Материал</span>
              <span>Партия</span>
              <span>Цена/ед.</span>
              <span>Сумма закупки</span>
              <span className="wh-col-name">Поставщик</span>
            </div>
            {filteredReceipts.map((r) => {
              const item = items.find((i) => i.id === r.itemId);
              return (
                <div className="wh-row wh-row-receipt" key={r.id}>
                  <span className="wh-mono wh-muted">{fmtDate(r.date)}</span>
                  <span className="wh-col-name">{item?.name || "—"}</span>
                  <span className="wh-mono">{item ? fmtQty(r.qty, item.unit) : num(r.qty)}</span>
                  <span className="wh-mono wh-strong">{rub(r.unitCost)}</span>
                  <span className="wh-mono">{rub(r.totalCost)}</span>
                  <span className="wh-col-name wh-col-wrap wh-muted" title={r.supplier || ""}>{r.supplier || "—"}</span>
                </div>
              );
            })}
            {filteredReceipts.length === 0 && <div className="wh-empty">Ничего не найдено</div>}
          </div>
        </>
      )}
    </div>
  );
}

const UNIT_OPTIONS = ["шт", "пара", "уп", "л", "кг"];

// Гарантированная раскладка "поле под полем" для форм — задаётся инлайн-стилем,
// а не CSS-классом, потому что в некоторых окружениях предпросмотра внешние
// правила для grid-template-columns на этой конкретной обёртке почему-то не
// применялись, хотя всё остальное на странице стилизовалось нормально.
// Инлайн-стиль имеет наивысший приоритет и не зависит от внешнего файла стилей.
const FORCE_STACK_STYLE = { display: "flex", flexDirection: "column", gap: 14 };

function ReceiveForm({ items, receipts = [], purchaseOptions = [], initialMode = "existing", onReceiveStock, onReceiveNewItem, onClose }) {
  const [mode, setMode] = useState(initialMode); // "existing" | "new"
  const [itemId, setItemId] = useState(items[0]?.id || "");
  const [supplier, setSupplier] = useState("");

  // Расчёт через упаковку: сколько единиц в одной закупленной упаковке,
  // сколько стоит упаковка и сколько таких упаковок пришло в этой партии.
  // Пример: тапочки — 100 шт в упаковке, упаковка стоит 3500 ₽, пришла 1 упаковка
  // → себестоимость единицы = 3500 / 100 = 35 ₽.
  const [packSize, setPackSize] = useState("");
  const [packPrice, setPackPrice] = useState("");
  const [packsCount, setPacksCount] = useState("1");

  // поля для случая, когда партия — это совсем новая позиция
  const [name, setName] = useState("");
  const [category, setCategory] = useState(CATEGORIES[0]);
  const [unit, setUnit] = useState(UNIT_OPTIONS[0]);
  const [minQuantity, setMinQuantity] = useState("");

  const packSizeNum = Number(packSize) || 0;
  const packPriceNum = Number(packPrice) || 0;
  const packsCountNum = Number(packsCount) || 0;

  const qtyNum = packSizeNum * packsCountNum;
  const totalNum = packPriceNum * packsCountNum;
  const unitPrice = packSizeNum > 0 ? packPriceNum / packSizeNum : 0;

  const existingItem = items.find((i) => i.id === itemId);

  // Последние закупки именно этой позиции — чтобы сразу было видно, у какого
  // поставщика и по какой цене брали её в прошлый раз, и не путаться, если
  // один и тот же товар покупали у разных поставщиков по разным ценам.
  const itemReceiptHistory = mode === "existing"
    ? receipts.filter((r) => r.itemId === itemId).sort((a, b) => b.date.localeCompare(a.date)).slice(0, 3)
    : [];

  // Список уже встречавшихся поставщиков — для автоподсказки при вводе,
  // чтобы один и тот же поставщик не расползался на разные варианты написания.
  const supplierSuggestions = [...new Set(receipts.map((r) => r.supplier).filter(Boolean))];

  // Самая выгодная известная цена по этой позиции — из реальных прошлых
  // закупок и из справочника "Варианты закупки" (ссылки, которые ещё не
  // покупали, но уже нашли). Если новая цена заметно выше — предупреждаем,
  // чтобы сотрудник случайно не переплатил, просто заказав "там, где удобно".
  const PRICE_WARNING_THRESHOLD = 0.15; // 15% дороже — уже повод обратить внимание
  const cheapestKnown = mode === "existing"
    ? [
        ...receipts.filter((r) => r.itemId === itemId).map((r) => ({ price: r.unitCost, source: r.supplier || "прошлая закупка" })),
        ...purchaseOptions.filter((o) => o.itemId === itemId && o.price > 0).map((o) => ({ price: o.price, source: o.source })),
      ].sort((a, b) => a.price - b.price)[0]
    : null;
  const priceIsHigh =
    cheapestKnown && unitPrice > 0 && unitPrice > cheapestKnown.price * (1 + PRICE_WARNING_THRESHOLD);
  const priceOverPercent = cheapestKnown && unitPrice > 0
    ? Math.round(((unitPrice - cheapestKnown.price) / cheapestKnown.price) * 100)
    : 0;

  const newAvgCost =
    mode === "existing" && existingItem && qtyNum > 0
      ? (existingItem.quantity * existingItem.unitCost + totalNum) / (existingItem.quantity + qtyNum)
      : null;

  const canSave =
    packSizeNum > 0 &&
    packPriceNum > 0 &&
    packsCountNum > 0 &&
    (mode === "existing" ? !!itemId : name.trim().length > 0);

  const submit = () => {
    if (!canSave) return;
    if (mode === "existing") {
      onReceiveStock(itemId, qtyNum, totalNum, supplier);
    } else {
      onReceiveNewItem(
        {
          name: name.trim(),
          category,
          unit,
          minQuantity: Number(minQuantity) || 0,
        },
        qtyNum,
        totalNum,
        supplier
      );
    }
    onClose();
  };

  return (
    <div className="wh-panel wh-panel-accent">
      <div className="wh-panel-title">
        <span className="wh-panel-title-icon"><Truck size={16} /> {mode === "new" ? "Новая позиция" : "Приёмка партии"}</span>
        <button className="wh-icon-btn" onClick={onClose}><X size={15} /></button>
      </div>

      <div className="wh-receive-toggle">
        <button className={mode === "existing" ? "active" : ""} onClick={() => setMode("existing")}>
          Пополнить остаток
        </button>
        <button className={mode === "new" ? "active" : ""} onClick={() => setMode("new")}>
          Новая позиция
        </button>
      </div>

      <div className="wh-hint-banner">
        Укажите, сколько единиц в одной закупочной упаковке и сколько она стоит — себестоимость одной единицы
        и всей партии посчитаются сами. Например: упаковка тапочек — 100 пар по 3500 ₽ за упаковку → 35 ₽/пара.
        Важно: «Ед. измерения» — это то, <strong>чем вы отгружаете товар со склада</strong> (парами, штуками, литрами),
        а не сама закупочная упаковка. Если упаковку не вскрывают и выдают целиком (как соль или сахар) — выбирайте «уп».
        «Мин. запас» тоже указывается в единицах отгрузки, а не в упаковках.
      </div>

      <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
        {mode === "existing" ? (
          <label className="wh-span-2">
            Материал
            <select value={itemId} onChange={(e) => setItemId(e.target.value)}>
              {items.map((i) => (
                <option key={i.id} value={i.id}>
                  {i.name} — сейчас {fmtQty(i.quantity, i.unit)} по {rub(i.unitCost)}
                </option>
              ))}
            </select>
          </label>
        ) : (
          <>
            <label className="wh-span-2">
              Наименование
              <input value={name} onChange={(e) => setName(e.target.value)} placeholder="Например, Жидкое мыло" />
            </label>
            <label>
              Категория
              <select value={category} onChange={(e) => setCategory(e.target.value)}>
                {CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)}
              </select>
            </label>
            <label>
              Ед. измерения
              <select value={unit} onChange={(e) => setUnit(e.target.value)}>
                {UNIT_OPTIONS.map((u) => <option key={u} value={u}>{u}</option>)}
              </select>
            </label>
            <label>
              Мин. запас, {unit}
              <input type="number" min="0" value={minQuantity} onChange={(e) => setMinQuantity(e.target.value)} placeholder={`В тех же единицах: ${unit}`} />
            </label>
          </>
        )}

        {mode === "existing" && itemReceiptHistory.length > 0 && (
          <div className="wh-span-2 wh-price-history">
            <div className="wh-price-history-title">Последние закупки этой позиции — чтобы не путать поставщиков и цены:</div>
            {itemReceiptHistory.map((r) => (
              <div className="wh-price-history-row" key={r.id}>
                <span className="wh-mono wh-muted">{fmtDate(r.date)}</span>
                <span className="wh-col-name">{r.supplier || "поставщик не указан"}</span>
                <span className="wh-mono wh-strong">{rub(r.unitCost)}/ед.</span>
              </div>
            ))}
          </div>
        )}

        <label>
          Единиц в одной упаковке
          <input type="number" min="0" value={packSize} onChange={(e) => setPackSize(e.target.value)} placeholder="Например, 100" />
        </label>
        <label>
          Стоимость упаковки, ₽
          <input type="number" min="0" value={packPrice} onChange={(e) => setPackPrice(e.target.value)} placeholder="Например, 3500" />
        </label>
        <label>
          Сколько упаковок пришло
          <input type="number" min="0" value={packsCount} onChange={(e) => setPacksCount(e.target.value)} />
        </label>
        <label className="wh-span-2">
          Поставщик / комментарий
          <input
            value={supplier}
            onChange={(e) => setSupplier(e.target.value)}
            placeholder="Начните вводить — подставим уже известных поставщиков"
            list="wh-supplier-suggestions"
          />
          <datalist id="wh-supplier-suggestions">
            {supplierSuggestions.map((s) => <option key={s} value={s} />)}
          </datalist>
        </label>
      </div>

      {packSizeNum > 0 && packPriceNum > 0 && packsCountNum > 0 && (
        <div className="wh-cost-calc">
          <div className="wh-cost-calc-row">
            <span>Себестоимость одной единицы</span>
            <span className="wh-mono wh-strong">{rub(unitPrice)}</span>
          </div>
          <div className="wh-cost-calc-row">
            <span>Всего в партии ({num(packsCountNum)} уп. × {num(packSizeNum)})</span>
            <span className="wh-mono wh-strong">{fmtQty(qtyNum, mode === "new" ? unit : (existingItem?.unit || ""))} на {rub(totalNum)}</span>
          </div>
          {mode === "existing" && existingItem && (
            <div className="wh-cost-calc-row">
              <span>Новая средняя себестоимость ({fmtQty(existingItem.quantity, existingItem.unit)} по {rub(existingItem.unitCost)} + {num(qtyNum)} по {rub(unitPrice)})</span>
              <span className="wh-mono wh-strong">{rub(newAvgCost)}</span>
            </div>
          )}
        </div>
      )}

      {priceIsHigh && (
        <div className="wh-price-warning">
          <AlertTriangle size={15} />
          <span>
            Внимание: цена {rub(unitPrice)} за единицу на <strong>{priceOverPercent}%</strong> выше самого выгодного
            известного варианта — {rub(cheapestKnown.price)} ({cheapestKnown.source}). Проверьте, нет ли варианта дешевле,
            прежде чем проводить приход.
          </span>
        </div>
      )}

      <div className="wh-panel-actions">
        <button className="wh-btn-ghost" onClick={onClose}>Отмена</button>
        <button className="wh-btn-primary" disabled={!canSave} onClick={submit}>
          {mode === "new" ? "Создать позицию" : "Провести приход"}
        </button>
      </div>
    </div>
  );
}

function EditItemForm({ item, onSave, onClose }) {
  const [name, setName] = useState(item.name);
  const [category, setCategory] = useState(item.category);
  const [unit, setUnit] = useState(item.unit);
  const [minQuantity, setMinQuantity] = useState(String(item.minQuantity));

  const canSave = name.trim().length > 0;

  const submit = () => {
    if (!canSave) return;
    onSave({
      name: name.trim(),
      category,
      unit,
      minQuantity: Number(minQuantity) || 0,
    });
  };

  return (
    <div className="wh-panel wh-panel-accent">
      <div className="wh-panel-title">
        <span className="wh-panel-title-icon"><Pencil size={16} /> Редактирование позиции</span>
        <button className="wh-icon-btn" onClick={onClose}><X size={15} /></button>
      </div>

      <div className="wh-hint-banner">
        Здесь меняются только название, категория, единица измерения и минимальный запас.
        Текущий остаток ({fmtQty(item.quantity, item.unit)}) и себестоимость ({rub(item.unitCost)}) не тронутся —
        их можно изменить только через выдачу или приёмку партии.
      </div>

      <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
        <label className="wh-span-2">
          Наименование
          <input value={name} onChange={(e) => setName(e.target.value)} />
        </label>
        <label>
          Категория
          <select value={category} onChange={(e) => setCategory(e.target.value)}>
            {CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)}
          </select>
        </label>
        <label>
          Ед. измерения
          <select value={unit} onChange={(e) => setUnit(e.target.value)}>
            {UNIT_OPTIONS.map((u) => <option key={u} value={u}>{u}</option>)}
            {!UNIT_OPTIONS.includes(unit) && <option value={unit}>{unit}</option>}
          </select>
        </label>
        <label>
          Мин. запас, {unit}
          <input type="number" min="0" value={minQuantity} onChange={(e) => setMinQuantity(e.target.value)} />
        </label>
      </div>

      <div className="wh-panel-actions">
        <button className="wh-btn-ghost" onClick={onClose}>Отмена</button>
        <button className="wh-btn-primary" disabled={!canSave} onClick={submit}>Сохранить изменения</button>
      </div>
    </div>
  );
}


// ---------- Issue ----------

function IssueView({ items, issues, objects, role, onIssue, onAddObject, onToggleObjectActive, onClearIssues }) {
  const [itemId, setItemId] = useState(items[0]?.id || "");
  const [objectId, setObjectId] = useState("");
  const [qty, setQty] = useState("1");
  const [note, setNote] = useState("");
  const [filter, setFilter] = useState("");
  const [showObjects, setShowObjects] = useState(false);
  const [newNumber, setNewNumber] = useState("");
  const [newName, setNewName] = useState("");
  const [confirmingClear, setConfirmingClear] = useState(false);

  const activeObjects = [...objects].filter((o) => o.active).sort((a, b) => a.number.localeCompare(b.number, "ru", { numeric: true }));
  const allObjectsSorted = [...objects].sort((a, b) => a.number.localeCompare(b.number, "ru", { numeric: true }));

  const item = items.find((i) => i.id === itemId);
  const selectedObject = objects.find((o) => o.id === objectId);
  const qtyNum = Number(qty) || 0;
  const overStock = item && qtyNum > item.quantity;
  const canSubmit = item && selectedObject && qtyNum > 0 && !overStock;

  const submit = () => {
    if (!canSubmit) return;
    onIssue(itemId, selectedObject.number, selectedObject.name, qtyNum, note);
    setQty("1");
    setNote("");
  };

  const addObjectSubmit = () => {
    if (!newNumber.trim() || !newName.trim()) return;
    onAddObject(newNumber, newName);
    setNewNumber("");
    setNewName("");
  };

  const withNames = issues.map((s) => ({
    ...s,
    itemName: items.find((i) => i.id === s.itemId)?.name || "—",
  }));

  const filteredLog = withNames.filter(
    (s) =>
      s.roomNumber.toLowerCase().includes(filter.toLowerCase()) ||
      (s.roomName || "").toLowerCase().includes(filter.toLowerCase()) ||
      s.itemName.toLowerCase().includes(filter.toLowerCase())
  );

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>Выдача материалов</h1>
          <p className="wh-sub">Оформите выдачу расходника с привязкой к объекту</p>
        </div>
        {role === "admin" && (
          <button className="wh-btn-secondary" onClick={() => setShowObjects((v) => !v)}>
            🏢 Управление объектами
          </button>
        )}
      </div>

      {role === "admin" && showObjects && (
        <div className="wh-panel">
          <div className="wh-panel-title">
            <span>Реестр объектов ({activeObjects.length} активных из {objects.length})</span>
            <button className="wh-icon-btn" onClick={() => setShowObjects(false)}><X size={15} /></button>
          </div>
          <div className="wh-hint-banner">
            Объекты синхронизированы с картой объектов. Когда объект выбывает — не удаляйте его, а выключайте
            переключателем: так все прошлые выдачи и отчёты по нему сохранят номер и название.
          </div>
          <div className="wh-form-grid" style={{ ...FORCE_STACK_STYLE, marginBottom: 16 }}>
            <label>
              Номер объекта
              <input value={newNumber} onChange={(e) => setNewNumber(e.target.value)} placeholder="Например, 88" />
            </label>
            <label className="wh-span-2">
              Название
              <input value={newName} onChange={(e) => setNewName(e.target.value)} placeholder="Название объекта" />
            </label>
            <div style={{ display: "flex", alignItems: "flex-end" }}>
              <button className="wh-btn-primary" onClick={addObjectSubmit}>Добавить</button>
            </div>
          </div>
          <div className="wh-objects-list">
            {allObjectsSorted.map((o) => (
              <div className={`wh-object-row ${o.active ? "" : "inactive"}`} key={o.id}>
                <span className="wh-mono wh-strong">{o.number}</span>
                <span className="wh-col-name">{o.name}</span>
                <button className="wh-btn-ghost" onClick={() => onToggleObjectActive(o.id)}>
                  {o.active ? "Отметить как выбывший" : "Вернуть в активные"}
                </button>
              </div>
            ))}
          </div>
        </div>
      )}

      <div className="wh-panel wh-panel-static">
        <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
          <label className="wh-span-2">
            Материал
            <select value={itemId} onChange={(e) => setItemId(e.target.value)}>
              {items.map((i) => (
                <option key={i.id} value={i.id} disabled={i.quantity === 0}>
                  {i.name} — остаток {fmtQty(i.quantity, i.unit)}{i.quantity === 0 ? " (нет в наличии)" : ""}
                </option>
              ))}
            </select>
          </label>
          <label>
            Объект
            <select value={objectId} onChange={(e) => setObjectId(e.target.value)}>
              <option value="">Выберите объект…</option>
              {activeObjects.map((o) => (
                <option key={o.id} value={o.id}>{o.number} — {o.name}</option>
              ))}
            </select>
          </label>
          <label>
            Количество{item ? `, ${item.unit}` : ""}
            <input type="number" min="1" value={qty} onChange={(e) => setQty(e.target.value)} />
          </label>
          <label className="wh-span-2">
            Комментарий (необязательно)
            <input value={note} onChange={(e) => setNote(e.target.value)} placeholder="Причина выдачи, № заявки…" />
          </label>
        </div>

        {overStock && (
          <div className="wh-warning-line">
            <AlertTriangle size={14} /> На складе только {fmtQty(item.quantity, item.unit)} — уменьшите количество
          </div>
        )}

        <div className="wh-panel-actions">
          <div className="wh-cost-preview">
            {item && qtyNum > 0 ? (
              <>Стоимость выдачи: <span className="wh-strong">{rub(qtyNum * item.unitCost)}</span></>
            ) : (
              <span className="wh-muted">Выберите материал, объект и количество</span>
            )}
          </div>
          <button className="wh-btn-primary" disabled={!canSubmit} onClick={submit}>
            Выдать материал
          </button>
        </div>
      </div>

      <div className="wh-log-header">
        <h2>📜 Журнал выдач</h2>
        <div className="wh-search wh-search-small">
          <Search size={14} />
          <input placeholder="Фильтр по объекту или материалу" value={filter} onChange={(e) => setFilter(e.target.value)} />
        </div>
        {role === "admin" && issues.length > 0 && (
          confirmingClear ? (
            <div className="wh-inline-confirm">
              <span>Удалить {issues.length} записей?</span>
              <button className="wh-btn-writeoff-submit" onClick={() => { onClearIssues(); setConfirmingClear(false); }}>Да, очистить</button>
              <button className="wh-btn-ghost" onClick={() => setConfirmingClear(false)}>Отмена</button>
            </div>
          ) : (
            <button className="wh-icon-btn-danger" onClick={() => setConfirmingClear(true)} title="Очистить журнал выдач">
              <Trash2 size={15} />
            </button>
          )
        )}
      </div>

      <div className="wh-table">
        <div className="wh-row wh-row-head wh-row-log">
          <span>Дата</span>
          <span>Объект</span>
          <span className="wh-col-name">Материал</span>
          <span>Кол-во</span>
          <span>Сумма</span>
          <span className="wh-col-name">Комментарий</span>
        </div>
        {filteredLog.map((s) => (
          <div className="wh-row wh-row-log" key={s.id}>
            <span className="wh-mono wh-muted">{fmtDate(s.date)}</span>
            <span className="wh-apt-badge" title={s.roomName}><BedDouble size={12} /> {s.roomNumber} · {s.roomName}</span>
            <span className="wh-col-name">{s.itemName}</span>
            <span className="wh-mono">{num(s.qty)}</span>
            <span className="wh-mono wh-strong">{rub(s.cost)}</span>
            <span className="wh-col-name wh-col-wrap wh-muted" title={s.note || ""}>{s.note || "—"}</span>
          </div>
        ))}
        {filteredLog.length === 0 && <div className="wh-empty">Записей не найдено</div>}
      </div>
    </div>
  );
}

// ---------- Assets (общескладской инвентарь) ----------

function AssetsView({ assets, transfers, objects, warehouses, role, onAddAssetType, onRestock, onRemoveAssetType, onTransfer, onAddWarehouse }) {
  const [showAddForm, setShowAddForm] = useState(false);
  const [newName, setNewName] = useState("");
  const [newQty, setNewQty] = useState("");

  const [showWarehousesPanel, setShowWarehousesPanel] = useState(false);
  const [newWarehouseName, setNewWarehouseName] = useState("");

  // actionFor: { id, mode } где mode = "transfer" | "writeoff"
  const [actionFor, setActionFor] = useState(null);
  const [fromKey, setFromKey] = useState("warehouse");
  const [toKey, setToKey] = useState("");
  const [moveQty, setMoveQty] = useState("");
  const [moveNote, setMoveNote] = useState("");

  const [restockForId, setRestockForId] = useState(null);
  const [restockQty, setRestockQty] = useState("");
  const [confirmingDeleteId, setConfirmingDeleteId] = useState(null);

  const [filter, setFilter] = useState("");

  const canOperate = role === "admin" || role === "warehouse";
  const isWarehouseKey = (key) => warehouses.some((w) => w.id === key);

  const activeObjects = [...objects].filter((o) => o.active).sort((a, b) => a.number.localeCompare(b.number, "ru", { numeric: true }));

  const resolveLabel = (key) => {
    if (key === "written_off") return "Списано";
    const wh = warehouses.find((w) => w.id === key);
    if (wh) return wh.name;
    const obj = objects.find((o) => o.number === key);
    if (obj) return `${key} — ${obj.name}`;
    const t = transfers.find((t) => t.fromKey === key || t.toKey === key);
    if (t) return `${key} — ${t.fromKey === key ? t.fromLabel : t.toLabel}`;
    return key;
  };

  const filteredAssets = assets.filter((a) => a.name.toLowerCase().includes(filter.toLowerCase()));

  const submitAdd = () => {
    if (!newName.trim() || !newQty) return;
    onAddAssetType(newName, Number(newQty) || 0);
    setNewName("");
    setNewQty("");
    setShowAddForm(false);
  };

  const submitAddWarehouse = () => {
    if (!newWarehouseName.trim()) return;
    onAddWarehouse(newWarehouseName);
    setNewWarehouseName("");
  };

  const submitRestock = (id) => {
    const n = Number(restockQty);
    if (!n || n <= 0) return;
    onRestock(id, n);
    setRestockForId(null);
    setRestockQty("");
  };

  const openAction = (assetId, mode) => {
    setActionFor({ id: assetId, mode });
    setFromKey("warehouse");
    setToKey("");
    setMoveQty("");
    setMoveNote("");
  };

  const submitAction = (asset, dist) => {
    const qty = Number(moveQty) || 0;
    if (qty <= 0 || qty > (dist[fromKey] || 0)) return;
    if (actionFor.mode === "writeoff") {
      onTransfer(asset.id, fromKey, resolveLabel(fromKey), "written_off", "Списано", qty, moveNote);
    } else {
      if (!toKey) return;
      onTransfer(asset.id, fromKey, resolveLabel(fromKey), toKey, resolveLabel(toKey), qty, moveNote);
    }
    setActionFor(null);
    setMoveQty("");
    setMoveNote("");
  };

  const historyWithNames = transfers.map((t) => ({
    ...t,
    assetName: assets.find((a) => a.id === t.assetId)?.name || "—",
  }));

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>🛏️ Общескладской инвентарь</h1>
          <p className="wh-sub">Многоразовое оборудование — где сейчас находится и куда перемещалось</p>
        </div>
        {role === "admin" && (
          <div className="wh-header-actions">
            <button className="wh-btn-icon-export" onClick={() => exportAssets(assets, transfers, objects, warehouses)} title="Выгрузить в Excel">
              <FileSpreadsheet size={18} />
            </button>
            <button className="wh-btn-secondary" onClick={() => setShowWarehousesPanel((v) => !v)}>
              <Warehouse size={16} /> Склады
            </button>
            <button className="wh-btn-primary" onClick={() => setShowAddForm((v) => !v)}>
              <Plus size={16} /> Добавить тип инвентаря
            </button>
          </div>
        )}
      </div>

      {showWarehousesPanel && (
        <div className="wh-panel">
          <div className="wh-panel-title">
            <span>Склады ({warehouses.length})</span>
            <button className="wh-icon-btn" onClick={() => setShowWarehousesPanel(false)}><X size={15} /></button>
          </div>
          <div className="wh-hint-banner">
            Список складов, между которыми можно перемещать инвентарь. Добавьте сюда ещё один, если открылся
            новый склад — он сразу появится в выборе «Откуда» / «Куда» у всех типов инвентаря.
          </div>
          <div className="wh-objects-list" style={{ marginBottom: 14 }}>
            {warehouses.map((w) => (
              <div className="wh-object-row" key={w.id} style={{ gridTemplateColumns: "1fr" }}>
                <span className="wh-col-name"><Warehouse size={13} style={{ marginRight: 6, verticalAlign: -2 }} />{w.name}</span>
              </div>
            ))}
          </div>
          <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
            <label className="wh-span-2">
              Название нового склада
              <input value={newWarehouseName} onChange={(e) => setNewWarehouseName(e.target.value)} placeholder="Например, Склад 3" />
            </label>
            <div style={{ display: "flex", alignItems: "flex-end" }}>
              <button className="wh-btn-primary" onClick={submitAddWarehouse}>Добавить склад</button>
            </div>
          </div>
        </div>
      )}

      {showAddForm && (
        <div className="wh-panel">
          <div className="wh-panel-title">
            Новый тип инвентаря
            <button className="wh-icon-btn" onClick={() => setShowAddForm(false)}><X size={15} /></button>
          </div>
          <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
            <label className="wh-span-2">
              Наименование
              <input value={newName} onChange={(e) => setNewName(e.target.value)} placeholder="Например, Раскладушка" />
            </label>
            <label>
              Количество на «{warehouses[0]?.name || "Склад 1"}»
              <input type="number" min="0" value={newQty} onChange={(e) => setNewQty(e.target.value)} placeholder="Например, 10" />
            </label>
          </div>
          <div className="wh-panel-actions">
            <button className="wh-btn-ghost" onClick={() => setShowAddForm(false)}>Отмена</button>
            <button className="wh-btn-primary" onClick={submitAdd}>Добавить</button>
          </div>
        </div>
      )}

      <div className="wh-search">
        <Search size={16} />
        <input placeholder="Найти по названию…" value={filter} onChange={(e) => setFilter(e.target.value)} />
      </div>

      <div className="wh-asset-list">
        {filteredAssets.map((asset) => {
          const dist = getAssetDistribution(asset, transfers);
          const locations = Object.entries(dist).filter(([key, qty]) => qty > 0 && key !== "written_off");
          const writtenOff = dist.written_off || 0;
          const isActing = actionFor?.id === asset.id;
          const isWriteOff = isActing && actionFor.mode === "writeoff";
          const isRestocking = restockForId === asset.id;

          return (
            <div className="wh-asset-card" key={asset.id}>
              <div className="wh-asset-head">
                <div>
                  <div className="wh-asset-name">{asset.name}</div>
                  <div className="wh-asset-total">
                    {num(asset.totalQuantity)} шт всего{writtenOff > 0 ? ` · списано ${num(writtenOff)}` : ""}
                  </div>
                </div>
                <div className="wh-header-actions">
                  {role === "admin" && (
                    <button className="wh-btn-ghost" onClick={() => { setRestockForId(isRestocking ? null : asset.id); setRestockQty(""); }}>
                      <PackagePlus size={14} /> Пополнить
                    </button>
                  )}
                  {canOperate && (
                    <>
                      <button className="wh-btn-secondary" onClick={() => (isActing && !isWriteOff ? setActionFor(null) : openAction(asset.id, "transfer"))}>
                        <ArrowRightLeft size={14} /> Переместить
                      </button>
                      <button className="wh-btn-writeoff" onClick={() => (isWriteOff ? setActionFor(null) : openAction(asset.id, "writeoff"))}>
                        <Trash2 size={14} /> Списать
                      </button>
                    </>
                  )}
                  {role === "admin" && (
                    confirmingDeleteId === asset.id ? (
                      <span className="wh-inline-confirm wh-inline-confirm-tight">
                        <span>Удалить?</span>
                        <button className="wh-btn-writeoff-submit" onClick={() => { onRemoveAssetType(asset.id); setConfirmingDeleteId(null); }}>Да</button>
                        <button className="wh-btn-ghost" onClick={() => setConfirmingDeleteId(null)}>Нет</button>
                      </span>
                    ) : (
                      <button
                        className="wh-icon-btn"
                        onClick={() => setConfirmingDeleteId(asset.id)}
                        title="Удалить тип инвентаря"
                      >
                        <X size={15} />
                      </button>
                    )
                  )}
                </div>
              </div>

              <div className="wh-asset-locations">
                {locations.length === 0 && <span className="wh-muted" style={{ fontSize: 12.5 }}>Нигде не числится</span>}
                {locations.map(([key, qty]) => (
                  <span className={`wh-asset-loc-badge ${isWarehouseKey(key) ? "warehouse" : ""}`} key={key}>
                    {isWarehouseKey(key) ? <Warehouse size={12} /> : <BedDouble size={12} />} {resolveLabel(key)}: <strong>{num(qty)}</strong>
                  </span>
                ))}
              </div>

              {isRestocking && (
                <div className="wh-asset-inline-form">
                  <input type="number" min="1" value={restockQty} onChange={(e) => setRestockQty(e.target.value)} placeholder="Сколько добавить" />
                  <button className="wh-btn-primary" onClick={() => submitRestock(asset.id)}>Добавить на «{warehouses[0]?.name || "Склад 1"}»</button>
                  <button className="wh-btn-ghost" onClick={() => setRestockForId(null)}>Отмена</button>
                </div>
              )}

              {isActing && (
                <div className="wh-asset-transfer-form">
                  <div className="wh-form-grid" style={FORCE_STACK_STYLE}>
                    <label>
                      Откуда{isWriteOff ? " (что списываем)" : ""}
                      <select value={fromKey} onChange={(e) => setFromKey(e.target.value)}>
                        {locations.map(([key, qty]) => (
                          <option key={key} value={key}>{resolveLabel(key)} (доступно {qty})</option>
                        ))}
                      </select>
                    </label>
                    {!isWriteOff && (
                      <label>
                        Куда
                        <select value={toKey} onChange={(e) => setToKey(e.target.value)}>
                          <option value="">Выберите…</option>
                          {warehouses.filter((w) => w.id !== fromKey).map((w) => (
                            <option key={w.id} value={w.id}>{w.name}</option>
                          ))}
                          {activeObjects.filter((o) => o.number !== fromKey).map((o) => (
                            <option key={o.id} value={o.number}>{o.number} — {o.name}</option>
                          ))}
                        </select>
                      </label>
                    )}
                    <label>
                      Количество
                      <input type="number" min="1" value={moveQty} onChange={(e) => setMoveQty(e.target.value)} />
                    </label>
                    <label className={isWriteOff ? "wh-span-2" : "wh-span-2"}>
                      {isWriteOff ? "Причина списания" : "Комментарий (необязательно)"}
                      <input
                        value={moveNote}
                        onChange={(e) => setMoveNote(e.target.value)}
                        placeholder={isWriteOff ? "Например, сломался вентилятор" : "Причина перемещения"}
                      />
                    </label>
                  </div>
                  <div className="wh-panel-actions">
                    <button className="wh-btn-ghost" onClick={() => setActionFor(null)}>Отмена</button>
                    <button className={isWriteOff ? "wh-btn-writeoff-submit" : "wh-btn-primary"} onClick={() => submitAction(asset, dist)}>
                      {isWriteOff ? "Списать" : "Переместить"}
                    </button>
                  </div>
                </div>
              )}
            </div>
          );
        })}
        {filteredAssets.length === 0 && <div className="wh-empty">Ничего не найдено</div>}
      </div>

      <div className="wh-log-header">
        <h2>📜 Журнал перемещений</h2>
      </div>
      <div className="wh-table">
        <div className="wh-row wh-row-head wh-row-transfer">
          <span>Дата</span>
          <span className="wh-col-name">Инвентарь</span>
          <span className="wh-col-name">Откуда</span>
          <span className="wh-col-name">Куда</span>
          <span>Кол-во</span>
          <span className="wh-col-name">Комментарий</span>
        </div>
        {historyWithNames.map((t) => (
          <div className="wh-row wh-row-transfer" key={t.id}>
            <span className="wh-mono wh-muted">{fmtDate(t.date)}</span>
            <span className="wh-col-name">{t.assetName}</span>
            <span className="wh-col-name">{t.fromLabel}</span>
            <span className="wh-col-name">{t.toLabel}</span>
            <span className="wh-mono">{num(t.quantity)}</span>
            <span className="wh-col-name wh-col-wrap wh-muted" title={t.note || ""}>{t.note || "—"}</span>
          </div>
        ))}
        {historyWithNames.length === 0 && <div className="wh-empty">Перемещений пока не было</div>}
      </div>
    </div>
  );
}

// ---------- Report ----------

// Пилюля-переключатель с выпадающим списком (год / месяц) — по образцу
// нативных календарных пикеров: кнопка со стрелкой открывает список,
// выбранный пункт отмечен галочкой.
function PillDropdown({ label, active, options, onSelect }) {
  const [open, setOpen] = useState(false);
  return (
    <div className="wh-pill-dropdown">
      <button className={`wh-pill-trigger ${active ? "active" : ""}`} onClick={() => setOpen((v) => !v)}>
        {label} <ChevronDown size={14} />
      </button>
      {open && (
        <>
          <div className="wh-pill-backdrop" onClick={() => setOpen(false)} />
          <div className="wh-pill-menu">
            {options.map((opt) => (
              <button
                key={opt.value}
                className={`wh-pill-menu-item ${opt.selected ? "selected" : ""} ${opt.trailing ? "trailing" : ""}`}
                onClick={() => { onSelect(opt.value); setOpen(false); }}
              >
                <span className="wh-pill-menu-check">{opt.selected && <Check size={13} />}</span>
                {opt.label}
              </button>
            ))}
          </div>
        </>
      )}
    </div>
  );
}

function ReportView({ items, issues, role, markupRate, onMarkupChange }) {
  const [period, setPeriod] = useState("all"); // "7d" | "30d" | "month" | "year" | "all"
  const [selectedYear, setSelectedYear] = useState(() => Number(todayISO().slice(0, 4)));
  const [selectedMonthIndex, setSelectedMonthIndex] = useState(() => Number(todayISO().slice(5, 7)) - 1); // 0-11
  const [rateInput, setRateInput] = useState(String(Math.round(markupRate * 100)));

  const selectedMonthKey = `${selectedYear}-${String(selectedMonthIndex + 1).padStart(2, "0")}`;

  const applyRate = () => {
    const n = Number(rateInput);
    if (!Number.isNaN(n) && n >= 0) onMarkupChange(n / 100);
  };

  const inRange = useMemo(() => {
    if (period === "7d" || period === "30d") {
      const days = period === "7d" ? 7 : 30;
      const d = new Date();
      d.setDate(d.getDate() - days);
      const cutoff = d.toISOString().slice(0, 10);
      return issues.filter((s) => s.date >= cutoff);
    }
    if (period === "month") return issues.filter((s) => s.date.slice(0, 7) === selectedMonthKey);
    if (period === "year") return issues.filter((s) => s.date.slice(0, 4) === String(selectedYear));
    return issues; // "all"
  }, [issues, period, selectedMonthKey, selectedYear]);

  const periodLabel = useMemo(() => {
    if (period === "7d") return "7 дней";
    if (period === "30d") return "30 дней";
    if (period === "year") return `${selectedYear} год`;
    if (period === "month") return `${MONTH_NAMES[selectedMonthIndex]} ${selectedYear}`;
    return "Всё время";
  }, [period, selectedYear, selectedMonthIndex]);

  // Список лет для пикера: несколько последних плюс любые годы, за которые
  // уже реально есть выдачи (на случай, если в данных есть более старые записи).
  const yearOptions = useMemo(() => {
    const nowYear = Number(todayISO().slice(0, 4));
    const years = new Set([nowYear, nowYear - 1, nowYear - 2]);
    issues.forEach((s) => years.add(Number(s.date.slice(0, 4))));
    return [...years].sort((a, b) => b - a);
  }, [issues]);

  const totalCost = inRange.reduce((s, x) => s + x.cost, 0);
  const totalQty = inRange.reduce((s, x) => s + x.qty, 0);
  const rooms = new Set(inRange.map((s) => s.roomNumber));
  const markupAmount = totalCost * markupRate;
  const billedTotal = totalCost + markupAmount;

  const [expandedRooms, setExpandedRooms] = useState(() => new Set());
  const toggleExpanded = (number) => {
    setExpandedRooms((prev) => {
      const next = new Set(prev);
      if (next.has(number)) next.delete(number); else next.add(number);
      return next;
    });
  };

  // Детализация по каждой выдаче — используется и в раскрытии карточки объекта,
  // и в выгрузке отчёта в Excel, чтобы везде была одна и та же разбивка.
  const detailedRows = useMemo(() => {
    return inRange
      .map((s) => {
        const logistics = s.cost * markupRate;
        return {
          date: s.date,
          roomNumber: s.roomNumber,
          roomName: s.roomName,
          itemName: items.find((i) => i.id === s.itemId)?.name || "—",
          qty: s.qty,
          cost: s.cost,
          logistics,
          total: s.cost + logistics,
        };
      })
      .sort((a, b) => b.date.localeCompare(a.date));
  }, [inRange, items, markupRate]);

  const byRoom = useMemo(() => {
    const map = new Map();
    inRange.forEach((s) => {
      const cur = map.get(s.roomNumber) || { roomNumber: s.roomNumber, roomName: s.roomName, cost: 0, qty: 0, issues: 0, lines: [] };
      cur.cost += s.cost;
      cur.qty += s.qty;
      cur.issues += 1;
      cur.lines.push(s);
      map.set(s.roomNumber, cur);
    });
    return [...map.values()]
      .map((r) => ({
        ...r,
        logistics: r.cost * markupRate,
        billed: r.cost * (1 + markupRate),
        lines: [...r.lines].sort((a, b) => b.date.localeCompare(a.date)),
      }))
      .sort((a, b) => b.billed - a.billed);
  }, [inRange, markupRate]);

  const byCategory = useMemo(() => {
    const map = new Map();
    inRange.forEach((s) => {
      const item = items.find((i) => i.id === s.itemId);
      const cat = item?.category || "Прочее";
      const cur = map.get(cat) || { cat, cost: 0 };
      cur.cost += s.cost;
      map.set(cat, cur);
    });
    return [...map.values()].sort((a, b) => b.cost - a.cost);
  }, [inRange, items]);

  const maxRoomCost = Math.max(1, ...byRoom.map((a) => a.billed));
  const maxCatCost = Math.max(1, ...byCategory.map((a) => a.cost));

  const handleExportReport = () => {
    exportReport(
      periodLabel,
      {
        totalCost,
        markupAmount,
        billedTotal,
        totalQty,
        roomsCount: rooms.size,
        markupPercent: Math.round(markupRate * 100),
      },
      byRoom,
      byCategory,
      detailedRows
    );
  };

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>📊 Отчёт по затратам</h1>
          <p className="wh-sub">Расход материалов и стоимость выдачи в разрезе номеров и категорий</p>
        </div>
        <div className="wh-report-controls">
          <div className="wh-period-switch">
            {[
              ["7d", "7 дней"],
              ["30d", "30 дней"],
              ["all", "Всё время"],
            ].map(([val, label]) => (
              <button
                key={val}
                className={period === val ? "active" : ""}
                onClick={() => setPeriod(val)}
              >
                {label}
              </button>
            ))}
          </div>

          <PillDropdown
            label={String(selectedYear)}
            active={period === "year"}
            options={yearOptions.map((y) => ({ value: y, label: String(y), selected: y === selectedYear }))}
            onSelect={(y) => { setSelectedYear(y); setPeriod("year"); }}
          />

          <PillDropdown
            label={period === "year" ? "Год (полностью)" : MONTH_NAMES[selectedMonthIndex]}
            active={period === "month"}
            options={[
              ...MONTH_NAMES.map((name, idx) => ({
                value: idx,
                label: name,
                selected: period === "month" && idx === selectedMonthIndex,
              })),
              { value: "__YEAR__", label: `Год (${MONTH_NAMES[0].slice(0, 3)}–${MONTH_NAMES[11].slice(0, 3)})`, selected: period === "year", trailing: true },
            ]}
            onSelect={(val) => {
              if (val === "__YEAR__") setPeriod("year");
              else { setSelectedMonthIndex(val); setPeriod("month"); }
            }}
          />

          <button className="wh-btn-icon-export" onClick={handleExportReport} title="Скачать отчёт в Excel">
            <FileSpreadsheet size={18} />
          </button>
        </div>
      </div>

      <div className="wh-markup-row">
        <div className="wh-markup-label">
          💡 Наценка на логистику: <strong>{Math.round(markupRate * 100)}%</strong> сверх реальной себестоимости расходников
        </div>
        {role === "admin" ? (
          <div className="wh-markup-edit">
            <input type="number" min="0" value={rateInput} onChange={(e) => setRateInput(e.target.value)} />
            <span>%</span>
            <button className="wh-btn-secondary" onClick={applyRate}>Применить</button>
          </div>
        ) : (
          <span className="wh-muted" style={{ fontSize: 12 }}>Ставку меняет администратор</span>
        )}
      </div>

      <div className="wh-stat-cards wh-stat-cards-5">
        <div className="wh-stat-card">
          <Package size={16} />
          <span className="wh-stat-card-value">{rub(totalCost)}</span>
          <span className="wh-stat-card-label">Себестоимость выдач</span>
        </div>
        <div className="wh-stat-card wh-stat-card-accent">
          <TrendingUp size={16} />
          <span className="wh-stat-card-value">{rub(markupAmount)}</span>
          <span className="wh-stat-card-label">Логистика ({Math.round(markupRate * 100)}%)</span>
        </div>
        <div className="wh-stat-card">
          <TrendingUp size={16} />
          <span className="wh-stat-card-value">{rub(billedTotal)}</span>
          <span className="wh-stat-card-label">Итого к выставлению</span>
        </div>
        <div className="wh-stat-card">
          <Package size={16} />
          <span className="wh-stat-card-value">{num(totalQty)}</span>
          <span className="wh-stat-card-label">Единиц выдано</span>
        </div>
        <div className="wh-stat-card">
          <BedDouble size={16} />
          <span className="wh-stat-card-value">{num(rooms.size)}</span>
          <span className="wh-stat-card-label">Номеров обслужено</span>
        </div>
      </div>

      <div className="wh-report-grid">
        <section className="wh-report-block">
          <h2>🏨 Расходы по объектам</h2>
          {byRoom.length === 0 && <div className="wh-empty">Нет данных за период</div>}
          {byRoom.map((a) => {
            const isExpanded = expandedRooms.has(a.roomNumber);
            return (
              <div className="wh-room-block" key={a.roomNumber}>
                <div className="wh-bar-row">
                  <button
                    className="wh-apt-badge wh-apt-badge-clickable"
                    onClick={() => toggleExpanded(a.roomNumber)}
                    title={isExpanded ? "Скрыть детализацию" : "Показать детализацию"}
                  >
                    <BedDouble size={12} /> {a.roomNumber}
                  </button>
                  <div className="wh-bar-track-wrap">
                    <div className="wh-bar-track">
                      <div className="wh-bar-fill" style={{ width: `${(a.billed / maxRoomCost) * 100}%` }} />
                    </div>
                    {isExpanded && <div className="wh-room-name-reveal">{a.roomName}</div>}
                  </div>
                  <span className="wh-mono wh-strong wh-bar-value">{rub(a.billed)}</span>
                  <span className="wh-mono wh-muted wh-bar-count">{a.issues} выдач</span>
                </div>

                {isExpanded && (
                  <div className="wh-room-detail">
                    <div className="wh-room-detail-row wh-room-detail-head">
                      <span>Дата</span>
                      <span className="wh-col-name">Материал</span>
                      <span>Кол-во</span>
                      <span>Себест.</span>
                      <span>Логистика</span>
                      <span>Итого</span>
                    </div>
                    {a.lines.map((line) => {
                      const itemName = items.find((i) => i.id === line.itemId)?.name || "—";
                      const logistics = line.cost * markupRate;
                      return (
                        <div className="wh-room-detail-row" key={line.id}>
                          <span className="wh-mono wh-muted">{fmtDate(line.date)}</span>
                          <span className="wh-col-name">{itemName}</span>
                          <span className="wh-mono">{num(line.qty)}</span>
                          <span className="wh-mono">{rub(line.cost)}</span>
                          <span className="wh-mono wh-muted">{rub(logistics)}</span>
                          <span className="wh-mono wh-strong">{rub(line.cost + logistics)}</span>
                        </div>
                      );
                    })}
                  </div>
                )}
              </div>
            );
          })}
        </section>

        <section className="wh-report-block">
          <h2>🗂️ Затраты по категориям</h2>
          {byCategory.length === 0 && <div className="wh-empty">Нет данных за период</div>}
          {byCategory.map((c) => (
            <div className="wh-bar-row" key={c.cat}>
              <span className="wh-cat-tag">{c.cat}</span>
              <div className="wh-bar-track">
                <div className="wh-bar-fill wh-bar-fill-alt" style={{ width: `${(c.cost / maxCatCost) * 100}%` }} />
              </div>
              <span className="wh-mono wh-strong wh-bar-value">{rub(c.cost)}</span>
            </div>
          ))}
        </section>
      </div>
    </div>
  );
}

// ---------- styles ----------

// ---------- Audit log (журнал изменений) ----------

// ---------- Price trends (динамика цен) ----------

function PriceTrendsView({ items, receipts }) {
  const [expandedId, setExpandedId] = useState(null);
  const [filter, setFilter] = useState("");

  // Для каждой позиции — история цен из реальных поступлений (не из
  // справочника вариантов закупки, там могут быть непроверенные цены).
  // Нужно минимум 2 закупки, чтобы вообще было что сравнивать.
  const trends = useMemo(() => {
    return items
      .map((item) => {
        const history = receipts
          .filter((r) => r.itemId === item.id)
          .sort((a, b) => a.date.localeCompare(b.date));
        if (history.length < 2) return null;
        const first = history[0];
        const last = history[history.length - 1];
        const change = ((last.unitCost - first.unitCost) / first.unitCost) * 100;
        return { item, history, first, last, change };
      })
      .filter(Boolean)
      .filter((t) => t.item.name.toLowerCase().includes(filter.toLowerCase()))
      .sort((a, b) => b.change - a.change);
  }, [items, receipts, filter]);

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>📈 Динамика цен</h1>
          <p className="wh-sub">
            Себестоимость позиций от первой зафиксированной закупки до последней — чтобы вовремя
            замечать, что что-то заметно подорожало
          </p>
        </div>
      </div>

      <div className="wh-search">
        <Search size={16} />
        <input placeholder="Найти по названию" value={filter} onChange={(e) => setFilter(e.target.value)} />
      </div>

      {trends.length === 0 && (
        <div className="wh-empty">
          Пока недостаточно данных. Динамика появится, как только по одной и той же позиции наберётся
          минимум две закупки в «Истории поступлений» — тогда будет с чем сравнивать.
        </div>
      )}

      <div className="wh-price-trend-list">
        {trends.map((t) => {
          const isUp = t.change > 0;
          const isBig = Math.abs(t.change) >= 15;
          const isExpanded = expandedId === t.item.id;
          const maxPoint = Math.max(...t.history.map((h) => h.unitCost));
          return (
            <div className="wh-price-trend-card" key={t.item.id}>
              <button className="wh-price-trend-head" onClick={() => setExpandedId(isExpanded ? null : t.item.id)}>
                <span className="wh-col-name wh-strong">{t.item.name}</span>
                <span className="wh-mono wh-muted">{rub(t.first.unitCost)} → {rub(t.last.unitCost)}</span>
                <span className={`wh-trend-badge ${isUp ? "up" : "down"} ${isBig ? "big" : ""}`}>
                  {isBig && isUp && "⚠️ "}{isUp ? "▲" : "▼"} {Math.abs(Math.round(t.change))}%
                </span>
              </button>
              {isExpanded && (
                <div className="wh-price-trend-history">
                  {t.history.map((r) => (
                    <div className="wh-price-trend-point" key={r.id}>
                      <span className="wh-mono wh-muted">{fmtDate(r.date)}</span>
                      <div className="wh-bar-track">
                        <div className="wh-bar-fill" style={{ width: `${(r.unitCost / maxPoint) * 100}%` }} />
                      </div>
                      <span className="wh-mono wh-strong">{rub(r.unitCost)}</span>
                      <span className="wh-col-name wh-muted">{r.supplier || "—"}</span>
                    </div>
                  ))}
                </div>
              )}
            </div>
          );
        })}
      </div>
    </div>
  );
}

function AuditLogView({ log, items, onRestoreItem, onRestoreLegacy }) {
  const [filter, setFilter] = useState("");
  const [justRestored, setJustRestored] = useState(() => new Set());
  const [legacyDone, setLegacyDone] = useState(false);

  const filtered = log.filter((entry) => {
    const q = filter.toLowerCase();
    return (
      entry.action.toLowerCase().includes(q) ||
      entry.details.toLowerCase().includes(q) ||
      (ROLES[entry.role]?.label || "").toLowerCase().includes(q)
    );
  });

  const handleRestore = (entry) => {
    onRestoreItem(entry.restorable.data);
    setJustRestored((prev) => new Set(prev).add(entry.id));
  };

  const handleRestoreLegacy = () => {
    onRestoreLegacy();
    setLegacyDone(true);
  };

  return (
    <div>
      <div className="wh-header">
        <div>
          <h1>📝 Журнал изменений</h1>
          <p className="wh-sub">
            Кто и когда менял каталог, объекты, инвентарь и склады — то, что не попадает
            ни в журнал выдач, ни в историю поступлений, ни в журнал перемещений
          </p>
        </div>
      </div>

      <div className="wh-legacy-restore">
        <span>
          🧹 7 позиций категории «Средства для уборки» были удалены ещё до появления кнопки «Восстановить»
          в этом журнале — их снимок для точечного восстановления не сохранился. Можно вернуть их разом.
        </span>
        <button className="wh-btn-ghost" onClick={handleRestoreLegacy} disabled={legacyDone}>
          {legacyDone ? "Возвращено" : "Вернуть 7 позиций"}
        </button>
      </div>

      <div className="wh-search">
        <Search size={16} />
        <input placeholder="Фильтр по действию, роли или деталям" value={filter} onChange={(e) => setFilter(e.target.value)} />
      </div>

      <div className="wh-table">
        <div className="wh-row wh-row-head wh-row-audit">
          <span>Когда</span>
          <span>Роль</span>
          <span className="wh-col-name">Действие</span>
          <span className="wh-col-name">Детали</span>
          <span></span>
        </div>
        {filtered.map((entry) => {
          const canRestoreItem =
            entry.restorable?.type === "item" &&
            !items.some((i) => i.id === entry.restorable.data.id);
          const restored = justRestored.has(entry.id);
          return (
            <div className="wh-row wh-row-audit" key={entry.id}>
              <span className="wh-mono wh-muted">{fmtDateTime(entry.date)}</span>
              <span>{ROLES[entry.role]?.emoji} {ROLES[entry.role]?.label || entry.role}</span>
              <span className="wh-col-name wh-strong">{entry.action}</span>
              <span className="wh-col-name wh-col-wrap wh-muted" title={entry.details}>{entry.details}</span>
              <span>
                {entry.restorable?.type === "item" && (
                  restored || !canRestoreItem ? (
                    <span className="wh-muted" style={{ fontSize: 11.5 }}>{restored ? "Восстановлено" : "Уже в каталоге"}</span>
                  ) : (
                    <button className="wh-btn-ghost" onClick={() => handleRestore(entry)}>Восстановить</button>
                  )
                )}
              </span>
            </div>
          );
        })}
        {filtered.length === 0 && <div className="wh-empty">{log.length === 0 ? "Изменений пока не было" : "Ничего не найдено"}</div>}
      </div>
    </div>
  );
}

const CSS = `
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700;800&family=Inter:wght@400;500;600;700&display=swap');

.wh-root {
  --bg:#EEF2F8;
  --white:#FFFFFF;
  --text:#232A3B;
  --muted:#94A0B8;
  --muted-2:#B4BECF;
  --line:#EEF1F6;

  --indigo:#5B6EF5;
  --indigo-dark:#4551D6;

  --orange-1:#FDBB53; --orange-2:#F58B6B;
  --blue-1:#6E86FF; --blue-2:#4A5FE0;
  --cyan-1:#3FD0E0; --cyan-2:#1FA9C4;
  --green-1:#54E0AA; --green-2:#22BF8B;
  --hero-1:#63E6B7; --hero-2:#2FAE8C;

  --pos:#22BF8B;
  --neg:#F0654F;

  --shadow-lg:0 24px 48px -16px rgba(35,42,59,0.14);
  --shadow-md:0 14px 32px -12px rgba(35,42,59,0.10);
  --shadow-sm:0 8px 18px -8px rgba(35,42,59,0.10);

  background: var(--bg);
  color: var(--text);
  font-family: 'Inter', sans-serif;
  font-size: 14px;
  line-height: 1.45;
  min-height: 100vh;
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
  padding: 24px 20px 40px;
  overflow-x: clip;
  -webkit-font-smoothing: antialiased;
}

/* На мобильном браузере, если хоть один элемент где-то в глубине верстки
   чуть шире экрана (например, строка таблицы, которая должна прокручиваться
   внутри своего блока), Safari иногда расширяет масштаб всей страницы вместо
   того чтобы просто обрезать этот элемент — тогда при открытии всё выглядит
   "увеличенным", и приходится вручную разводить пальцы, чтобы вернуть
   нормальный масштаб. overflow-x: clip надёжнее, чем hidden, против именно
   этого поведения (hidden в некоторых версиях Safari всё равно позволяет
   внутреннему широкому элементу повлиять на начальный масштаб страницы). */
html, body, #root {
  margin: 0;
  min-height: 100%;
  width: 100%;
  max-width: 100vw;
  overflow-x: clip;
  background: #EEF2F8;
}

.wh-page { max-width: 1180px; margin: 0 auto; }

.wh-save-error {
  display: flex; align-items: center; gap: 9px;
  background: #FDEDEA; color: #A83A26; border: 1px solid #F3C4B8;
  border-radius: 14px; padding: 12px 16px; margin-bottom: 16px;
  font-size: 12.5px; font-weight: 600; line-height: 1.5;
}

.wh-legacy-restore {
  display: flex; align-items: center; gap: 14px; justify-content: space-between; flex-wrap: wrap;
  background: #F6F8FC; border: 1px solid var(--line);
  border-radius: 14px; padding: 12px 16px; margin-bottom: 18px;
  font-size: 12.5px; color: var(--text); line-height: 1.5;
}
.wh-legacy-restore span { max-width: 560px; }
.wh-legacy-restore .wh-btn-ghost:disabled { opacity: .5; cursor: default; }

/* ---- login gate ---- */
.wh-login-root { display: flex; align-items: center; justify-content: center; padding: 40px 20px; }
.wh-login-card {
  background: var(--white); border-radius: 26px; box-shadow: var(--shadow-lg);
  padding: 36px 34px 30px; width: 100%; max-width: 340px;
  display: flex; flex-direction: column; align-items: center; text-align: center;
}
.wh-login-logo { width: 56px; height: 56px; border-radius: 16px; overflow: hidden; box-shadow: var(--shadow-sm); margin-bottom: 18px; }
.wh-login-logo img { width: 100%; height: 100%; object-fit: cover; display: block; }
.wh-login-title { font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 17px; margin-bottom: 4px; }
.wh-login-sub { font-size: 12.5px; color: var(--muted); font-weight: 500; margin-bottom: 24px; }
.wh-login-field { display: flex; flex-direction: column; gap: 6px; font-size: 12.5px; color: var(--muted); font-weight: 600; width: 100%; text-align: left; margin-bottom: 14px; }
.wh-login-field select, .wh-login-field input {
  font: inherit; font-size: 13.5px; color: var(--text); font-family: 'Inter', sans-serif;
  border: 1px solid var(--line); border-radius: 12px; padding: 10px 12px; background: #F9FAFD;
}
.wh-login-field select:focus, .wh-login-field input:focus { outline: 2px solid var(--indigo); outline-offset: 0; border-color: var(--indigo); }
.wh-login-error { color: var(--neg); font-size: 12.5px; font-weight: 600; margin-bottom: 14px; }
.wh-login-submit { width: 100%; justify-content: center; margin-top: 6px; }

.wh-mono { font-family: 'Inter', sans-serif; font-variant-numeric: tabular-nums; font-size: 13px; }
.wh-muted { color: var(--muted); }
.wh-strong { font-weight: 700; }

/* ---- hero sheet ---- */
.wh-hero-sheet { position: relative; background: var(--white); border-radius: 26px; box-shadow: var(--shadow-lg); padding: 22px 24px 24px; margin-bottom: 18px; }
.wh-hero-nav { display: flex; align-items: center; flex-wrap: wrap; gap: 14px; margin-bottom: 20px; }
.wh-brand { display: flex; align-items: center; gap: 10px; }
.wh-brand-badge {
  width: 36px; height: 36px; border-radius: 11px;
  background: #EEF0FF; color: var(--indigo-dark);
  display: flex; align-items: center; justify-content: center;
  box-shadow: var(--shadow-sm); flex-shrink: 0;
  overflow: hidden;
}
.wh-brand-badge img { width: 100%; height: 100%; object-fit: cover; display: block; }
.wh-brand-title { font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 15px; line-height: 1.2; }
.wh-brand-sub { font-size: 11.5px; color: var(--muted); font-weight: 500; margin-top: 2px; }

/* Обёртка вкладок — растягивается на всё оставшееся место и режет
   переполнение, чтобы затухающий край подсказывал: тут можно листать вбок. */
.wh-tab-pill-wrap { position: relative; flex: 1 1 auto; min-width: 0; overflow: hidden; border-radius: 999px; }
.wh-tab-pill-wrap::after {
  content: ""; position: absolute; top: 0; right: 0; bottom: 0; width: 28px;
  background: linear-gradient(to right, rgba(246,248,252,0), #F6F8FC 80%);
  pointer-events: none;
}
.wh-tab-pill { display: inline-flex; gap: 4px; background: #F6F8FC; border-radius: 999px; padding: 4px; max-width: 100%; overflow-x: auto; }
.wh-tab-pill button {
  border: none; background: transparent; cursor: pointer;
  font-family: 'Inter', sans-serif; font-weight: 600; font-size: 13px;
  padding: 9px 16px; border-radius: 999px; color: var(--muted); white-space: nowrap;
  transition: background .15s ease, color .15s ease;
}
.wh-tab-pill button:hover { color: var(--indigo-dark); }
.wh-tab-pill button.active { background: var(--indigo); color: #fff; box-shadow: var(--shadow-sm); }

/* Бейдж роли — обычный элемент строки навигации (не "плавающий" поверх),
   поэтому браузер сам резервирует под него место при любой ширине экрана
   и любом количестве вкладок — раньше абсолютное позиционирование иногда
   давало наложение на вкладки при большом их числе на широких экранах. */
.wh-role-badge {
  display: inline-flex; align-items: center; gap: 10px; font-size: 12.5px; color: var(--text); font-weight: 600;
  background: #F6F8FC; border-radius: 999px; padding: 7px 8px 7px 14px;
  margin-left: auto; flex-shrink: 0;
}
.wh-logout-btn { border: none; background: #fff; color: var(--muted); font: inherit; font-size: 11.5px; font-weight: 600; padding: 6px 12px; border-radius: 999px; cursor: pointer; box-shadow: var(--shadow-sm); }
.wh-logout-btn:hover { color: var(--neg); }

/* ---- wallet cards ---- */
.wh-wallet-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 20px; }
.wh-wallet-card {
  border-radius: 20px; padding: 20px 20px 22px; color: #fff; box-shadow: var(--shadow-md);
  position: relative; overflow: hidden;
  border: none; text-align: left; font-family: inherit; cursor: pointer;
  display: block; width: 100%;
  transition: transform .15s ease, box-shadow .15s ease;
}
.wh-wallet-card:hover { transform: translateY(-2px); box-shadow: var(--shadow-lg); }
.wh-wallet-card:active { transform: translateY(0); }
.wh-wallet-card.orange { background: linear-gradient(145deg, var(--orange-1), var(--orange-2)); }
.wh-wallet-card.blue { background: linear-gradient(145deg, var(--blue-1), var(--blue-2)); }
.wh-wallet-card.cyan { background: linear-gradient(145deg, var(--cyan-1), var(--cyan-2)); }
.wh-wallet-card.green { background: linear-gradient(145deg, var(--green-1), var(--green-2)); }
.wh-wallet-icon { font-size: 19px; width: 38px; height: 38px; border-radius: 12px; background: rgba(255,255,255,0.28); display: flex; align-items: center; justify-content: center; margin-bottom: 18px; }
.wh-wallet-card .w-label { font-size: 12.5px; font-weight: 600; opacity: .92; margin-bottom: 10px; }
.wh-wallet-card .w-value { font-size: 19px; font-weight: 700; font-family: 'Poppins', sans-serif; margin-bottom: 4px; }
.wh-wallet-card .w-sub { font-size: 11px; opacity: .85; font-weight: 500; }

/* ---- main ---- */
.wh-main { }

.wh-header { display: flex; justify-content: space-between; align-items: flex-end; gap: 16px; margin-bottom: 18px; flex-wrap: wrap; }
.wh-header h1 { font-family: 'Poppins', sans-serif; font-size: 20px; font-weight: 700; margin: 0 0 4px; }
.wh-sub { color: var(--muted); font-size: 13px; margin: 0; font-weight: 500; }
h2 { font-family: 'Poppins', sans-serif; font-size: 15px; font-weight: 600; margin: 0 0 14px; color: var(--text); }

.wh-btn-primary {
  display: inline-flex; align-items: center; gap: 7px;
  background: var(--indigo); color: #fff; border: none;
  font-family: 'Inter', sans-serif; font-weight: 600; font-size: 13.5px;
  padding: 11px 18px; border-radius: 999px; cursor: pointer;
  box-shadow: var(--shadow-sm);
  transition: background .15s ease; white-space: nowrap;
}
.wh-btn-primary:hover { background: var(--indigo-dark); }
.wh-btn-primary:disabled { opacity: .4; cursor: not-allowed; box-shadow: none; }

.wh-btn-ghost {
  background: transparent; border: 1px solid var(--line); color: var(--text);
  font-family: 'Inter', sans-serif; font-weight: 500; font-size: 13.5px;
  padding: 11px 18px; border-radius: 999px; cursor: pointer;
}

.wh-btn-icon-export {
  display: inline-flex; align-items: center; justify-content: center;
  width: 42px; height: 42px; flex-shrink: 0;
  background: #E9F7EF; border: 1px solid #BFE8CE; color: #1E7A44;
  border-radius: 999px; cursor: pointer; box-shadow: var(--shadow-sm);
  transition: background .15s ease, transform .15s ease;
}
.wh-btn-icon-export:hover { background: #DAF1E3; transform: translateY(-1px); }
.wh-btn-icon-export:active { transform: translateY(0); }

.wh-header-actions { display: flex; gap: 10px; flex-wrap: wrap; }
.wh-btn-secondary {
  display: inline-flex; align-items: center; gap: 7px;
  background: #EEF0FF; color: var(--indigo-dark); border: none;
  font-family: 'Inter', sans-serif; font-weight: 600; font-size: 13.5px;
  padding: 11px 18px; border-radius: 999px; cursor: pointer;
  white-space: nowrap;
}
.wh-btn-secondary:hover { background: #E4E9FB; }

.wh-btn-writeoff {
  display: inline-flex; align-items: center; gap: 7px;
  background: #FDEDEA; color: var(--neg); border: none;
  font-family: 'Inter', sans-serif; font-weight: 600; font-size: 13.5px;
  padding: 11px 18px; border-radius: 999px; cursor: pointer; white-space: nowrap;
}
.wh-btn-writeoff:hover { background: #FBDFD9; }
.wh-btn-writeoff-submit {
  display: inline-flex; align-items: center; gap: 7px;
  background: var(--neg); color: #fff; border: none;
  font-family: 'Inter', sans-serif; font-weight: 600; font-size: 13.5px;
  padding: 11px 18px; border-radius: 999px; cursor: pointer;
}
.wh-btn-writeoff-submit:hover { background: #D6553F; }

.wh-inline-confirm {
  display: inline-flex; align-items: center; gap: 10px;
  background: #FDEDEA; border-radius: 999px; padding: 8px 8px 8px 16px;
  font-size: 12.5px; font-weight: 600; color: var(--neg);
}
.wh-inline-confirm-tight { padding: 4px 4px 4px 10px; gap: 6px; font-size: 12px; }
.wh-inline-confirm .wh-btn-writeoff-submit, .wh-inline-confirm .wh-btn-ghost { padding: 6px 12px; font-size: 12px; }

.wh-search {
  display: flex; align-items: center; gap: 8px;
  background: #F6F8FC; border: 1px solid var(--line);
  border-radius: 999px; padding: 10px 16px; margin-bottom: 18px;
  color: var(--muted); max-width: 420px;
}
.wh-search input { border: none; outline: none; background: transparent; font: inherit; color: var(--text); width: 100%; font-family: 'Inter', sans-serif; }
.wh-search-small { max-width: 280px; padding: 7px 12px; margin-bottom: 0; }

.wh-filter-chip {
  display: inline-flex; align-items: center; gap: 8px;
  background: #EEF0FF; color: var(--indigo-dark); font-size: 12.5px; font-weight: 600;
  padding: 7px 8px 7px 14px; border-radius: 999px; margin-bottom: 18px;
}
.wh-filter-chip button { border: none; background: rgba(255,255,255,0.6); color: var(--indigo-dark); border-radius: 999px; width: 20px; height: 20px; display: inline-flex; align-items: center; justify-content: center; cursor: pointer; }
.wh-filter-chip button:hover { background: #fff; }
.wh-filter-chip.warn { background: #FDEDEA; color: var(--neg); }
.wh-filter-chip.warn button { color: var(--neg); }

/* ---- panel / form ---- */
.wh-panel { width: 100%; max-width: 100%; box-sizing: border-box; background: var(--white); border-radius: 22px; box-shadow: var(--shadow-sm); padding: 22px 24px; margin-bottom: 22px; }
.wh-panel-static { margin-bottom: 30px; }
.wh-panel-title { display: flex; justify-content: space-between; align-items: center; font-family: 'Poppins', sans-serif; font-weight: 600; font-size: 14.5px; margin-bottom: 16px; }
.wh-panel-title-icon { display: inline-flex; align-items: center; gap: 8px; }
.wh-panel-accent { box-shadow: var(--shadow-sm), 0 0 0 2px var(--orange-2) inset; }

.wh-receive-toggle { display: inline-flex; gap: 4px; background: #F6F8FC; border-radius: 999px; padding: 4px; margin-bottom: 16px; }
.wh-receive-toggle button { border: none; background: transparent; font: inherit; font-size: 12.5px; font-weight: 600; padding: 8px 16px; border-radius: 999px; cursor: pointer; color: var(--muted); }
.wh-receive-toggle button.active { background: var(--indigo); color: #fff; }

.wh-hint-banner { background: #F6F8FC; border: 1px solid var(--line); border-radius: 14px; padding: 12px 16px; margin-bottom: 16px; font-size: 12.5px; color: var(--text); line-height: 1.5; }

.wh-price-history { background: #F6F8FC; border: 1px solid var(--line); border-radius: 14px; padding: 12px 14px; display: flex; flex-direction: column; gap: 6px; }
.wh-price-history-title { font-size: 11.5px; color: var(--muted); font-weight: 600; margin-bottom: 2px; }
.wh-price-history-row { display: grid; grid-template-columns: 80px 1fr auto; gap: 10px; align-items: center; font-size: 12.5px; }

.wh-cost-calc { background: #FFF7E8; border: 1px dashed var(--orange-2); border-radius: 14px; padding: 12px 16px; margin-top: 14px; display: flex; flex-direction: column; gap: 6px; }

.wh-price-warning {
  display: flex; align-items: flex-start; gap: 10px;
  background: #FDEDEA; border: 1px solid #F3C4B8; color: #A83A26;
  border-radius: 14px; padding: 12px 16px; margin-top: 14px;
  font-size: 12.5px; line-height: 1.5;
}
.wh-price-warning strong { font-family: 'Poppins', sans-serif; }

/* ---- price trends ---- */
.wh-price-trend-list { display: flex; flex-direction: column; gap: 10px; }
.wh-price-trend-card { background: var(--white); border-radius: 16px; box-shadow: var(--shadow-sm); overflow: hidden; }
.wh-price-trend-head {
  display: grid; grid-template-columns: 1.6fr 160px 110px; align-items: center; gap: 14px;
  width: 100%; border: none; background: transparent; cursor: pointer;
  padding: 16px 18px; font-family: inherit; text-align: left;
}
.wh-price-trend-head:hover { background: #F9FAFD; }
.wh-trend-badge {
  display: inline-flex; align-items: center; justify-content: center; gap: 4px;
  font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 12.5px;
  padding: 6px 12px; border-radius: 999px; justify-self: end;
}
.wh-trend-badge.up { background: #FDEDEA; color: var(--neg); }
.wh-trend-badge.down { background: #E3F7EE; color: var(--pos); }
.wh-trend-badge.big.up { background: var(--neg); color: #fff; }

.wh-price-trend-history { padding: 4px 18px 16px; display: flex; flex-direction: column; gap: 8px; border-top: 1px solid var(--line); padding-top: 14px; }
.wh-price-trend-point { display: grid; grid-template-columns: 90px 1fr 90px 1.2fr; align-items: center; gap: 12px; font-size: 12.5px; }
.wh-cost-calc-row { display: flex; justify-content: space-between; gap: 16px; font-size: 12.5px; color: #9A5B12; }
.wh-cost-calc-row span:first-child { max-width: 70%; }

.wh-form-grid { display: grid; grid-template-columns: 1fr; gap: 14px; }
.wh-span-2 { grid-column: span 2; }
.wh-form-grid label { display: flex; flex-direction: column; gap: 5px; font-size: 12.5px; color: var(--muted); font-weight: 600; }
.wh-form-grid input, .wh-form-grid select {
  font: inherit; font-size: 13.5px; color: var(--text); font-family: 'Inter', sans-serif;
  border: 1px solid var(--line); border-radius: 12px; padding: 9px 12px; background: #F9FAFD;
}
.wh-form-grid input:focus, .wh-form-grid select:focus { outline: 2px solid var(--indigo); outline-offset: 0; border-color: var(--indigo); }

.wh-panel-actions { display: flex; justify-content: space-between; align-items: center; margin-top: 18px; gap: 12px; flex-wrap: wrap; }
.wh-cost-preview { font-size: 13.5px; }
.wh-warning-line { display: flex; align-items: center; gap: 7px; color: var(--neg); font-size: 13px; margin-top: 12px; font-weight: 600; }

/* ---- catalog table ---- */
.wh-cat-block { margin-bottom: 18px; }
.wh-cat-header {
  display: flex; align-items: center; gap: 8px; width: 100%;
  background: transparent; border: none; cursor: pointer;
  padding: 10px 6px; font-family: inherit; color: var(--text);
}
.wh-cat-name { font-family: 'Poppins', sans-serif; font-weight: 600; font-size: 14px; }
.wh-cat-count { background: #EEF0FF; color: var(--indigo-dark); font-size: 11.5px; font-weight: 700; padding: 2px 9px; border-radius: 999px; }
.wh-cat-value { margin-left: auto; font-family: 'Poppins', sans-serif; font-size: 13px; color: var(--muted); font-weight: 600; }

.wh-table { width: 100%; max-width: 100%; background: var(--white); border-radius: 20px; box-shadow: var(--shadow-sm); overflow-x: auto; overflow-y: hidden; -webkit-overflow-scrolling: touch; }
.wh-row {
  display: grid; grid-template-columns: 2fr 132px 60px 90px 100px 130px;
  align-items: center; gap: 10px; padding: 12px 16px;
  border-bottom: 1px solid var(--line); font-size: 13.5px; font-family: 'Inter', sans-serif;
  min-width: 600px;
}
.wh-row-log { grid-template-columns: 92px 96px 1.6fr 70px 90px 1.6fr; min-width: 560px; }
.wh-row-receipt { grid-template-columns: 92px 1.8fr 110px 90px 110px 1.4fr; min-width: 620px; }
.wh-row:last-child { border-bottom: none; }
.wh-row-head { background: #F8F9FC; color: var(--muted); font-size: 11px; font-weight: 700; letter-spacing: .03em; text-transform: uppercase; padding: 12px 16px; }
.wh-row-low { background: #FDF1EF; }
.wh-col-name { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.wh-col-wrap { white-space: normal; word-break: break-word; line-height: 1.4; align-self: center; }

.wh-qty-cell { display: flex; align-items: center; gap: 6px; }
.wh-qty-btn {
  width: 22px; height: 22px; border-radius: 7px; border: 1px solid var(--line);
  background: #F6F8FC; cursor: pointer; font-size: 13px; line-height: 1; color: var(--text);
  display: flex; align-items: center; justify-content: center;
}
.wh-qty-btn:hover { background: #EEF0FF; border-color: var(--indigo); }

.wh-row-actions { display: flex; align-items: center; gap: 8px; justify-content: flex-end; }

.wh-icon-btn-active { background: #EEF0FF; }
.wh-link-count {
  display: inline-flex; align-items: center; justify-content: center;
  min-width: 15px; height: 15px; border-radius: 999px; margin-left: 2px;
  background: var(--indigo); color: #fff; font-size: 9.5px; font-weight: 700; padding: 0 3px;
}

.wh-purchase-links { background: #F9FAFD; border-bottom: 1px solid var(--line); padding: 14px 16px 16px; }
.wh-purchase-links-list { display: flex; flex-direction: column; gap: 4px; margin-bottom: 12px; }
.wh-purchase-link-row {
  display: grid; grid-template-columns: 1.6fr 90px 1.4fr 90px 28px;
  align-items: center; gap: 10px; padding: 7px 10px; font-size: 12.5px;
  background: var(--white); border-radius: 10px;
}
.wh-purchase-link-row a { color: var(--indigo-dark); text-decoration: underline; }
.wh-purchase-link-form { display: grid; grid-template-columns: 1fr; gap: 8px; }
.wh-purchase-link-form input {
  font: inherit; font-size: 12.5px; border: 1px solid var(--line); border-radius: 10px;
  padding: 8px 10px; background: var(--white);
}
.wh-purchase-link-form .wh-btn-primary { padding: 8px 16px; font-size: 12.5px; }
.wh-badge-low { display: inline-flex; align-items: center; gap: 4px; color: var(--neg); font-size: 11px; font-weight: 700; background: #FDEDEA; padding: 3px 8px; border-radius: 999px; }
.wh-icon-btn { background: none; border: none; color: var(--muted); cursor: pointer; padding: 5px; border-radius: 8px; display: inline-flex; }
.wh-icon-btn:hover { background: #EEF0FF; color: var(--indigo-dark); }

.wh-icon-btn-danger {
  display: inline-flex; align-items: center; justify-content: center;
  width: 34px; height: 34px; border-radius: 999px; flex-shrink: 0;
  background: #FDEDEA; border: 1px solid #F3C4B8; color: var(--neg); cursor: pointer;
  transition: background .15s ease;
}
.wh-icon-btn-danger:hover { background: #FBDFD9; }

.wh-empty { padding: 26px; text-align: center; color: var(--muted); font-size: 13.5px; }

/* ---- assets (общескладской инвентарь) ---- */
.wh-asset-list { display: flex; flex-direction: column; gap: 14px; margin-bottom: 30px; }
.wh-asset-card { background: var(--white); border-radius: 18px; box-shadow: var(--shadow-sm); padding: 18px 20px; }
.wh-asset-head { display: flex; justify-content: space-between; align-items: flex-start; gap: 14px; flex-wrap: wrap; margin-bottom: 12px; }
.wh-asset-name { font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 15px; }
.wh-asset-total { font-size: 12px; color: var(--muted); font-weight: 600; margin-top: 2px; }
.wh-asset-locations { display: flex; flex-wrap: wrap; gap: 8px; }
.wh-asset-loc-badge {
  display: inline-flex; align-items: center; gap: 5px;
  background: #E4E9FB; color: var(--indigo-dark); font-size: 12px; font-weight: 600;
  padding: 5px 11px; border-radius: 999px;
}
.wh-asset-loc-badge.warehouse { background: #EEF0FF; color: var(--text); }
.wh-asset-loc-badge strong { font-family: 'Poppins', sans-serif; }

.wh-asset-inline-form { display: flex; align-items: center; gap: 8px; margin-top: 14px; flex-wrap: wrap; }
.wh-asset-inline-form input {
  font: inherit; font-size: 13.5px; border: 1px solid var(--line); border-radius: 10px;
  padding: 8px 12px; background: #F9FAFD; width: 160px;
}
.wh-asset-transfer-form { margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--line); }

.wh-row-transfer { grid-template-columns: 92px 1.3fr 1.3fr 1.3fr 70px 1.4fr; min-width: 680px; }
.wh-row-audit { grid-template-columns: 140px 170px 1.2fr 1.8fr 120px; min-width: 760px; }

/* ---- issue log ---- */
.wh-log-header { display: flex; justify-content: space-between; align-items: center; margin: 28px 0 14px; flex-wrap: wrap; gap: 10px; }
.wh-apt-badge {
  display: inline-flex; align-items: center; gap: 5px; width: fit-content;
  background: #E4E9FB; color: var(--indigo-dark); font-weight: 700; font-size: 12.5px;
  padding: 4px 10px; border-radius: 999px;
}
.wh-apt-badge-clickable { border: none; cursor: pointer; font-family: inherit; }
.wh-apt-badge-clickable:hover { background: #D7DEFA; }

/* ---- object management ---- */
.wh-objects-list { display: flex; flex-direction: column; gap: 6px; max-height: 340px; overflow-y: auto; }
.wh-object-row { display: grid; grid-template-columns: 48px 1fr auto; align-items: center; gap: 12px; padding: 8px 10px; border-radius: 10px; background: #F9FAFD; }
.wh-object-row.inactive { opacity: .55; }
.wh-object-row .wh-btn-ghost { padding: 6px 12px; font-size: 12px; }

/* ---- report ---- */
.wh-report-controls { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
.wh-period-switch { display: flex; gap: 4px; background: #F6F8FC; border-radius: 999px; padding: 4px; }
.wh-period-switch button { border: none; background: transparent; font: inherit; font-size: 12.5px; font-weight: 600; padding: 8px 14px; border-radius: 999px; cursor: pointer; color: var(--muted); }
.wh-period-switch button.active { background: var(--indigo); color: #fff; }
.wh-pill-dropdown { position: relative; }
.wh-pill-trigger {
  display: inline-flex; align-items: center; gap: 8px;
  font: inherit; font-size: 12.5px; font-weight: 600; color: var(--text);
  border: 1px solid var(--line); border-radius: 999px; padding: 8px 14px;
  background: var(--white); cursor: pointer; white-space: nowrap;
}
.wh-pill-trigger.active { border-color: var(--indigo); color: var(--indigo-dark); box-shadow: 0 0 0 1px var(--indigo) inset; }
.wh-pill-backdrop { position: fixed; inset: 0; z-index: 20; }
.wh-pill-menu {
  position: absolute; top: calc(100% + 6px); right: 0; z-index: 21;
  background: var(--white); border-radius: 14px; box-shadow: var(--shadow-lg);
  padding: 6px; min-width: 190px; max-height: 320px; overflow-y: auto;
  display: flex; flex-direction: column; gap: 1px;
}
.wh-pill-menu-item {
  display: flex; align-items: center; gap: 8px; text-align: left;
  border: none; background: transparent; font: inherit; font-size: 13px; font-weight: 500;
  color: var(--text); padding: 9px 10px; border-radius: 9px; cursor: pointer; white-space: nowrap;
}
.wh-pill-menu-item:hover { background: #F0F2FA; }
.wh-pill-menu-item.selected { background: var(--indigo); color: #fff; font-weight: 600; }
.wh-pill-menu-item.trailing { margin-top: 5px; border-top: 1px solid var(--line); padding-top: 11px; border-radius: 0 0 9px 9px; }
.wh-pill-menu-check { width: 14px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }

.wh-stat-cards { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 26px; }
.wh-stat-cards-5 { grid-template-columns: repeat(5, 1fr); }
.wh-stat-card { background: var(--white); border-radius: 18px; box-shadow: var(--shadow-sm); padding: 18px 20px; display: flex; flex-direction: column; gap: 8px; color: var(--indigo-dark); }
.wh-stat-card-accent { background: #FFF7E8; box-shadow: var(--shadow-sm), 0 0 0 1px var(--orange-2) inset; }
.wh-stat-card-value { font-family: 'Poppins', sans-serif; font-size: 21px; font-weight: 700; color: var(--text); }
.wh-stat-card-label { font-size: 12px; color: var(--muted); font-weight: 600; }

.wh-markup-row { display: flex; justify-content: space-between; align-items: center; gap: 16px; flex-wrap: wrap; background: var(--white); border-radius: 16px; box-shadow: var(--shadow-sm); padding: 14px 18px; margin-bottom: 18px; }
.wh-markup-label { font-size: 13px; color: var(--text); }
.wh-markup-label strong { font-family: 'Poppins', sans-serif; color: var(--indigo-dark); }
.wh-markup-edit { display: flex; align-items: center; gap: 6px; }
.wh-markup-edit input { width: 64px; font: inherit; font-size: 13.5px; border: 1px solid var(--line); border-radius: 10px; padding: 8px 10px; background: #F9FAFD; text-align: right; }
.wh-markup-edit span { font-size: 13px; color: var(--muted); font-weight: 600; }

.wh-report-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; align-items: start; }
.wh-report-block { background: var(--white); border-radius: 20px; box-shadow: var(--shadow-sm); padding: 20px 22px; }

.wh-bar-row { display: grid; grid-template-columns: 74px 1fr 92px 64px; align-items: center; gap: 10px; padding: 8px 0; }
.wh-report-block:last-child .wh-bar-row { grid-template-columns: 118px 1fr 92px; }
.wh-bar-track { height: 8px; background: var(--line); border-radius: 20px; overflow: hidden; }
.wh-bar-track-wrap { display: flex; flex-direction: column; gap: 4px; }
.wh-room-name-reveal { font-size: 11.5px; color: var(--muted); font-weight: 600; }

.wh-room-block { border-bottom: 1px solid var(--line); }
.wh-room-block:last-child { border-bottom: none; }
.wh-room-block .wh-bar-row { border-bottom: none; }
.wh-room-detail { padding: 4px 0 12px 84px; display: flex; flex-direction: column; gap: 2px; }
.wh-room-detail-row { display: grid; grid-template-columns: 70px 1.6fr 56px 76px 76px 76px; gap: 8px; align-items: center; padding: 5px 0; font-size: 12px; }
.wh-room-detail-head { color: var(--muted); font-size: 10.5px; font-weight: 700; text-transform: uppercase; letter-spacing: .02em; padding-bottom: 6px; }
.wh-bar-fill { height: 100%; border-radius: 20px; background: linear-gradient(90deg, var(--orange-1), var(--orange-2)); }
.wh-bar-fill-alt { background: linear-gradient(90deg, var(--blue-1), var(--indigo-dark)); }
.wh-bar-value { text-align: right; font-family: 'Poppins', sans-serif; }
.wh-bar-count { text-align: right; font-size: 11.5px; color: var(--muted); }
.wh-cat-tag { font-size: 12.5px; font-weight: 600; color: var(--text); }

@media (max-width: 900px) {
  .wh-wallet-row { grid-template-columns: repeat(2, 1fr); }
  .wh-report-grid { grid-template-columns: 1fr; }
  .wh-stat-cards { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 560px) {
  .wh-wallet-row { grid-template-columns: repeat(2, 1fr); gap: 10px; }
  .wh-wallet-card { padding: 14px 14px 16px; border-radius: 16px; }
  .wh-wallet-icon { width: 28px; height: 28px; font-size: 14px; border-radius: 9px; margin-bottom: 10px; }
  .wh-wallet-card .w-label { font-size: 11px; margin-bottom: 6px; }
  .wh-wallet-card .w-value { font-size: 15px; margin-bottom: 2px; }
  .wh-wallet-card .w-sub { font-size: 9.5px; }
  .wh-stat-cards { grid-template-columns: 1fr; }
  .wh-hero-nav { flex-direction: column; align-items: flex-start; }
  .wh-tab-pill-wrap { width: 100%; flex: 1 1 100%; }
  .wh-role-badge {
    margin-left: 0; margin-top: 4px;
    padding: 5px 6px 5px 10px; gap: 6px;
  }
  .wh-role-badge span { font-size: 11px; }
  .wh-logout-btn { padding: 5px 9px; font-size: 10.5px; }
  .wh-purchase-link-row { grid-template-columns: 1fr; row-gap: 4px; }
  .wh-cost-calc-row { flex-direction: column; align-items: flex-start; gap: 2px; }
  .wh-panel { padding: 16px 16px; }
  .wh-hint-banner { font-size: 11.5px; padding: 10px 12px; margin-bottom: 12px; }
  .wh-cost-calc { padding: 10px 12px; }
  .wh-price-history { padding: 10px 12px; }
  .wh-price-history-row { grid-template-columns: 1fr; row-gap: 2px; }
  .wh-form-grid label { gap: 3px; }
  .wh-form-grid input, .wh-form-grid select { padding: 8px 10px; }
  .wh-panel-actions {
    position: sticky; bottom: 0; margin: 16px -16px -16px;
    padding: 12px 16px; background: var(--white);
    border-top: 1px solid var(--line); border-radius: 0 0 22px 22px;
  }
}
`;
